Thank you for providing the file! I can see exactly where the misleading messages are coming from.
In your JournalRequestApproval component, there are two functions that handle approvals: handleSubmitAction (for the whole batch) and handleSubmitPartialAction (for selected rows). Both of these are currently hardcoding a "success" message without reading the backend's response.
Here are the exact replacements you need to make in this file to fix the issue:
### 1. Update handleSubmitAction (Lines ~255 - 280)
Find your handleSubmitAction function and update the try block to capture the backend's response and use an "info" snackbar instead of "success".
**Change this:**
```javascript
    try {
      await callApi(
        "/JS/journals/process-bulk",
        {
          batchId: selectedBatch.batchId,
          status: modalMode,
          remarks: executorRemarks.trim(),
        },
        "POST"
      );
      showSnackBar(
        modalMode === "ACCEPTED"
          ? "Request Accepted Successfully"
          : "Request Rejected Successfully",
        "success"
      );
      handleCloseModal();
      fetchPendingBatches();
    } catch (e) {

```
**To this:**
```javascript
    try {
      const response = await callApi(
        "/JS/journals/process-bulk",
        {
          batchId: selectedBatch.batchId,
          status: modalMode,
          remarks: executorRemarks.trim(),
        },
        "POST"
      );
      
      // FIX: Read the backend message or default to async processing message
      const displayMessage = response?.message || "Approval process started in background. You will be notified upon completion.";
      showSnackBar(displayMessage, "info");
      
      handleCloseModal();
      fetchPendingBatches();
    } catch (e) {

```
### 2. Update handleSubmitPartialAction (Lines ~295 - 325)
Do the exact same thing for the partial submission logic.
**Change this:**
```javascript
    try {
      await callApi(
        "/JS/journals/process-bulk",
        {
          batchId: viewingBatchId,
          journalIdPrefixes: selectedJournalPrefixes,
          status: mode,
          remarks: partialExecutorRemarks.trim(),
        },
        "POST"
      );
      showSnackBar(
        mode === "ACCEPTED"
          ? "Selected Entries Accepted Successfully"
          : "Selected Entries Rejected Successfully",
        "success"
      );
      handleCloseViewModal();
    } catch (e) {

```
**To this:**
```javascript
    try {
      const response = await callApi(
        "/JS/journals/process-bulk",
        {
          batchId: viewingBatchId,
          journalIdPrefixes: selectedJournalPrefixes,
          status: mode,
          remarks: partialExecutorRemarks.trim(),
        },
        "POST"
      );
      
      // FIX: Read the backend message or default to async processing message
      const displayMessage = response?.message || "Partial approval process started in background. You will be notified upon completion.";
      showSnackBar(displayMessage, "info");
      
      handleCloseViewModal();
    } catch (e) {

```
By making these two changes, the moment a user clicks "Confirm", the UI will immediately dismiss the modal and show a blue "Info" popup stating that the process has started in the background. It perfectly aligns the frontend UX with what your backend is actually doing!
