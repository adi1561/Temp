I see exactly why it is crashing.
Your application is crashing immediately on load with a ReferenceError because the variables isINR and amountRegex are being used in the Amount field, but they were never actually defined at the top of your rows.map loop!
Because of the bracket issue we discussed earlier, it looks like that piece of logic got left out. Let's fix this right now so the page renders properly.
Here are the **two exact replacements** you need to make to fix the crash.
### Step 1: Define the variables at the start of the loop
**Find this exact line (around line 372):**
```javascript
        <Box>
          {rows.map((row, index) => (
            <React.Fragment key={row.id}>
              <Grid container spacing={2} alignItems="flex-start">

```
**Replace it with this:**
```javascript
        <Box>
          {rows.map((row, index) => {
            // Dynamic regex logic for Amount based on selected currency
            const isINR = row.currency === "INR";
            const amountRegex = isINR ? /^\d{0,16}(\.\d{0,2})?$/ : /^\d{0,16}(\.\d{0,4})?$/;

            return (
            <React.Fragment key={row.id}>
              <Grid container spacing={2} alignItems="flex-start">

```
### Step 2: Close the new brackets at the end of the loop
Because we changed => ( to => { return (, we have to close them properly at the bottom.
**Scroll down to the end of the mapping loop (around line 608) and find this:**
```javascript
              {index < rows.length - 1 && (
                <Box
                  sx={{
                    borderBottom: 1,
                    borderColor: "divider",
                    my: 2,
                  }}
                />
              )}
            </React.Fragment>
          ))}

```
**Replace it with this:**
```javascript
              {index < rows.length - 1 && (
                <Box
                  sx={{
                    borderBottom: 1,
                    borderColor: "divider",
                    my: 2,
                  }}
                />
              )}
            </React.Fragment>
            );
          })}

```
Save the file after making these two changes. The variables will now be properly defined for each row, and your frontend will stop crashing!
