I understand. Let's get very specific. To solve the "bugs from the screen" issue, we need to replace generic catch (Exception e) blocks with specific ones. This prevents the backend from swallowing critical errors or sending a generic "500 Internal Server Error" to the UI.
Here are the exact line-by-line replacements for your service to meet Rule 1.
📂 File: JournalBulkValidationService.java
1. Method: saveStatusToRedis
 * Location: Around Line 364.
 * Original Code:
   } catch (Exception e) {
    log.error("Failed to save status to Redis for {}", reqId, e);
}

 * Corrected Code:
   } catch (org.springframework.data.redis.RedisSystemException | org.springframework.dao.DataAccessException e) {
    log.error("Redis storage failure for ReqID: {}", reqId, e);
    // Do not throw; let the process continue if possible, but log the specific DB error
}

2. Method: parseAmount
 * Location: Around Line 457.
 * Original Code:
   } catch (Exception e) {
    d.amount = BigDecimal.ZERO;
}

 * Corrected Code:
   } catch (NumberFormatException | ArithmeticException e) {
    log.warn("Invalid numeric format at row {}: {}", d.rowIndex, amountRaw);
    d.amount = BigDecimal.ZERO;
    d.formatErrors.add("Invalid amount format: " + amountRaw);
}

3. Method: validateSystemColumns
 * Location: Around Line 477.
 * Original Code:
   } catch (Exception e) {
    d.formatErrors.add("Invalid calendar date");
}

 * Corrected Code:
   } catch (java.time.format.DateTimeParseException e) {
    log.warn("Date parsing failed for row {}: {}", d.rowIndex, cDate);
    d.formatErrors.add("Date must be in ddMMyyyy format");
}

📂 File: JournalRequestServiceImpl.java
1. Method: executeAsyncBatchCreation
 * Location: Around Line 608.
 * Original Code:
   } catch (Exception e) {
    log.error("Async Create Failed for Batch {}", batchId, e);
    // ... cleanup logic ...
}

 * Corrected Code:
   } catch (org.springframework.dao.DataAccessException e) {
    log.error("Database persistence failure for Batch {}: {}", batchId, e.getMostSpecificCause().getMessage());
    handleCreationFailure(batchId, creatorId, requestId, "Database Error: " + e.getMostSpecificCause().getMessage());
} catch (java.util.concurrent.CompletionException e) {
    log.error("Parallel thread execution failed for Batch {}", batchId, e);
    handleCreationFailure(batchId, creatorId, requestId, "Internal system processing error");
}

2. Method: executeAsyncBatchProcessing
 * Location: Around Line 666.
 * Original Code:
   } catch (Exception e) {
    log.error("Async Process Failed for Batch {}", batchId, e);
    // ... error update logic ...
}

 * Corrected Code:
   } catch (java.sql.SQLException e) {
    log.error("Oracle PL/SQL execution failed for Batch {}. SQL State: {}, Error: {}", batchId, e.getSQLState(), e.getMessage());
    String safeMsg = "Database Procedure Error: " + e.getMessage();
    jdbcTemplate.update("UPDATE JOURNAL_BATCH_MASTER SET BATCH_STATUS = 'ERROR', EXECUTOR_REMARKS = ? WHERE BATCH_ID = ?", safeMsg, batchId);
    progressService.sendProgress(batchId, executorId, 0, "FAILED", safeMsg);
} catch (org.springframework.dao.DataAccessException e) {
    log.error("Network or Connection failure during Batch {} processing", batchId, e);
    // Handle specifically for connection drops
}

3. Method: getCurrentPostingDate
 * Location: Around Line 710.
 * Original Code:
   } catch (Exception e) {
    log.error("Error fetching USERS_DATE. Using System Date.", e);
    return LocalDate.now();
}

 * Corrected Code:
   } catch (org.springframework.dao.EmptyResultDataAccessException e) {
    log.warn("FINCORE_DATE table is empty. Defaulting to System Date.");
    return LocalDate.now();
} catch (org.springframework.jdbc.CannotGetJdbcConnectionException e) {
    log.error("Database connection unavailable while fetching posting date");
    throw new CustomExceptions.DatabaseOperationException("Database unavailable", e);
}

📂 File: ProgressService.java
1. Method: sendProgress
 * Location: Around Line 776.
 * Original Code:
   } catch (Exception e) {
    log.warn("Progress Notification Failed: {}", e.getMessage());
}

 * Corrected Code:
   } catch (com.fasterxml.jackson.core.JsonProcessingException e) {
    log.error("Failed to serialize progress for Task {}: {}", taskId, e.getMessage());
} catch (org.springframework.data.redis.RedisConnectionFailureException e) {
    log.warn("Redis connection lost. Throttling progress update for Task {}", taskId);
}

Why this fixes your stress:
When a bug happens now, your logs won't just say java.lang.Exception. They will say exactly what happened (e.g., DateTimeParseException or EmptyResultDataAccessException). This allows you to fix the "screen errors" instantly because you'll know if the user sent a bad date or if the database is actually down.
Would you like me to move to Point 2: Descriptive Logging and show you how to implement the "never log PII" rule for these specific blocks?
