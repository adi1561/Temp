As a Tech Lead, I have to stop you right there: **we absolutely must change the frontend.** Here is why: even if you fix the Oracle database or the Java backend to safely send the massive number as a perfect text string ("2469135780242468.6912"), your *existing* React code will immediately destroy it.
Take a look at your current React function:
const formatCurrency = (num) => Number(num).toFixed(4);
The moment your code wraps that perfect string in **Number(num)**, JavaScript forces it back into its 64-bit math engine. It will instantly corrupt the string and round it right back to 2469135780242468.5000 before drawing it on the screen.
To protect the data, you **must** update your React file to format the currency using pure string manipulation, completely bypassing JavaScript's math engine.
### The Required Frontend Change
At the top of your React file, replace your existing formatCurrency function with this updated version:
```javascript
// Pure string manipulation to completely bypass JavaScript's 64-bit float limit
const formatCurrency = (val) => {
  if (val === null || val === undefined || val === "") return "0.0000";
  
  // Ensure it's a string without using Number()
  const strVal = String(val).trim();
  
  // Split into integer and decimal parts
  const parts = strVal.split(".");
  let intPart = parts[0];
  let decPart = parts[1] || "";

  // Pad or truncate to exactly 4 decimal places
  decPart = decPart.padEnd(4, "0").substring(0, 4);

  // Safely add commas to the integer part (e.g. 1000000 -> 1,000,000)
  intPart = intPart.replace(/\B(?=(\d{3})+(?!\d))/g, ",");

  return `${intPart}.${decPart}`;
};

```
If you apply the backend fix (sending it as a string) **and** this frontend fix (formatting it as a string), your UI will finally be able to display numbers into the quadrillions with perfect decimal precision!
