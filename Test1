As a Lead, I will give you exactly the lines you need to change in those working files to inject the runId for reconciliation.
Here are the precise changes required across the 3 files.
1. Changes in LhoTransferStrategy.java
Inside your handleLhoApproval method, you need to extract the batchId (which holds your runId from the frontend) and add it to both the payload and the publisher call.
Add these lines right after you extract processRunId:
// Extract runId (sent as 'batchId' from React)
String runId = originalPayload.has("batchId") ? originalPayload.get("batchId").asText() : null;

Add this line where you are putting items into customKafkaPayload:
customKafkaPayload.put("runId", runId);

Update your publishLhoTransferEvent method call to pass the runId:
// Pass the runId as the 3rd argument
kafkaEventPublisherService.publishLhoTransferEvent(request.getTargetId(), request.getId(), runId, customKafkaPayload);

2. Changes in KafkaEventPublisherService.java
You need to update the method signature to accept the runId and attach it as a Kafka Header so downstream systems can read it without deserializing the payload.
Update the method signature:
public void publishLhoTransferEvent(String targetId, Long requestId, String runId, Object payload) {

Add this right below where you add the requestId header:
// Attach runId to headers for tracebility
if (runId != null) {
    record.headers().add("runId", runId.getBytes(java.nio.charset.StandardCharsets.UTF_8));
}

3. Changes in LhoKafkaConsumerService.java (Optional but Recommended)
Since the whole point of sending the runId is reconciliation, Airflow will likely send it back to you in its response. You should extract it and log it so you can trace the exact run that succeeded or failed.
Add this helper method at the bottom of the class:
private String extractRunId(JsonNode payload) {
    if (payload.has("runId") && !payload.get("runId").isNull()) {
        return payload.get("runId").asText();
    }
    return "UNKNOWN_RUN_ID";
}

Then, inside both consumeResponseOne and consumeResponseTwo, call it and add it to your logs:
String runId = extractRunId(payload);

// Update your log.info / log.warn statements to include it:
log.info("Topic Success received for Request ID: {}. Run ID: {}. Marking as ACCEPTED.", requestId, runId);

Make these surgical updates, and your event-driven architecture will have perfect traceability. Are you ready to trigger a test transfer from the UI to verify Airflow receives the runId?
