That's my bad—I suggested using InvalidDataException to harden the controller, but we haven't added that specific inner class to your CustomExceptions.java file yet.
To fix the "Can't resolve symbol" error, you need to add the following static inner class to your CustomExceptions file. This allows the controller to throw a clean 400 Bad Request when a user sends a malformed date or invalid field.
Update: CustomExceptions.java
Add this block inside your CustomExceptions class:
/**
 * Rule 1: Specific Exception Types.
 * Added InvalidDataException to handle 400 Bad Request scenarios 
 * like malformed dates or invalid field values from the frontend.
 */
@ResponseStatus(HttpStatus.BAD_REQUEST)
public static class InvalidDataException extends RuntimeException {
    public InvalidDataException(String message) {
        super(message);
    }
}

Full Context for CustomExceptions.java
Your file should now look like this to support all the "Hardened" logic we've implemented:
package com.fincore.JournalService.Exception;

import org.springframework.http.HttpStatus;
import org.springframework.web.bind.annotation.ResponseStatus;

public class CustomExceptions {

    @ResponseStatus(HttpStatus.NOT_FOUND)
    public static class CacheExpiredException extends RuntimeException {
        public CacheExpiredException(String message) { super(message); }
    }

    @ResponseStatus(HttpStatus.INTERNAL_SERVER_ERROR)
    public static class FileProcessingException extends RuntimeException {
        public FileProcessingException(String message) { super(message); }
        public FileProcessingException(String message, Throwable cause) { super(message, cause); }
    }

    @ResponseStatus(HttpStatus.INTERNAL_SERVER_ERROR)
    public static class BatchProcessingException extends RuntimeException {
        public BatchProcessingException(String message) { super(message); }
        public BatchProcessingException(String message, Throwable cause) { super(message, cause); }
    }

    @ResponseStatus(HttpStatus.BAD_REQUEST)
    public static class DataMappingException extends RuntimeException {
        public DataMappingException(String message) { super(message); }
    }

    // NEW: Resolves the "Can't resolve symbol" error
    @ResponseStatus(HttpStatus.BAD_REQUEST)
    public static class InvalidDataException extends RuntimeException {
        public InvalidDataException(String message) { super(message); }
    }
}

Why this is the correct "Lead" approach:
By mapping this to HttpStatus.BAD_REQUEST, your GlobalExceptionHandler will automatically return a 400 status code to the frontend. This is much better than a 500 error because it tells the user (and the mobile/web developers) that the mistake was on their side (wrong date format), not a server crash.
Once you add this, the red lines in your JournalRequestController will disappear.
Is there anything else throwing a compilation error, or are we clear to run the server?
