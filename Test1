I see exactly what is happening. Take a deep breath—you provided the perfect debugging information. I can confidently tell you that your Java code is 100% correct, and this is entirely an infrastructure issue.
Let's break down exactly what your terminal output just revealed.
### 1. Why did the second kubectl command fail?
Your first command (kubectl get pods) worked perfectly, but the second one (kubectl get svc) failed with that localhost:8080 error.
This happened because **you forgot to add the --kubeconfig flag** to the second command. Without that flag, your terminal tried to look for a local Kubernetes cluster on your computer instead of connecting to your remote server.
**Run this exact command to see the service:**
```bash
kubectl get svc hdfs-namenode-1-svc -n cbops --kubeconfig h06vksuatcbopscls.conf

```
### 2. The Real Reason for "Connection Refused"
Look very closely at the output of your successful kubectl get pods command.
You have Airflow running, Grafana, Kubernetes Dashboards, and your spark-thrift pod.
**But there are absolutely NO HDFS pods running in your cbops namespace.** There is no namenode, and there are no datanodes.
This is exactly why Spark threw the Connection refused error! Your Spark Thrift server is trying to send the journal data to the HDFS Namenode, but **the HDFS database does not exist in this namespace.** Spark is knocking on a door where nobody is home.
### 3. The Secondary Issue: Hive Schematool Errors
I also notice in your pod list that you have several hive-schematool pods sitting in an **Error** state from 23 hours ago. This means when your infrastructure was spun up yesterday, the Hive Metastore failed to initialize properly (likely because it couldn't connect to the Postgres database or HDFS).
### Your Immediate Next Steps
Since you are on a tight deadline, you need to pivot away from your Java code and look at your Kubernetes cluster deployment:
 1. **Find HDFS:** Did someone deploy Hadoop/HDFS into a different namespace? (Run kubectl get pods -A --kubeconfig h06vksuatcbopscls.conf | grep namenode to search the whole cluster).
 2. **Deploy HDFS:** If HDFS is completely missing, you (or your DevOps team) need to deploy the HDFS Namenode and Datanodes into the cbops namespace.
 3. **Fix the Service URL:** If HDFS is intentionally hosted on a completely different server (outside of Kubernetes), then your core-site.xml configuration has the wrong IP address. It is currently pointing to an internal Kubernetes service (10-177-103-199.hdfs-namenode-1-svc.cbops.svc.cluster.local) that doesn't seem to have any pods attached to it.
Your Java backend is doing exactly what it is supposed to do. It successfully talked to Spark, and now Spark is complaining that its hard drive (HDFS) is missing. You just need to get HDFS running!
