SELECT p.MENU_ID, p.MAPPED_REQUEST_TYPE, p.MENU_ACTION, rp.ROLE_ID, r.ROLE_STATUS
FROM MENU_ITEMS p
JOIN ROLE_PERMISSIONS rp ON p.MENU_ID = rp.PERMISSION_ID
JOIN ROLES r ON rp.ROLE_ID = r.ROLE_ID
WHERE p.MAPPED_REQUEST_TYPE = 'JOURNAL_AUTH';
