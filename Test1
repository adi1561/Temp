You need to make this change inside the **JournalRequestServiceImpl.java** file, specifically within the **executeAsyncBatchCreation** method.
Because we are upgrading to AtomicReference<BigDecimal>, we also get to delete the old "Precision Recovery" division logic at the bottom of that method, since BigDecimal handles the decimal placement natively without needing to be scaled down by 10,000!
Here are the exact replacements to make inside executeAsyncBatchCreation:
### 1. Update the Initializations
Find these lines (around the middle of the method, right before the for loop):
```java
            // High-precision thread-safe counters for financial totals
            LongAdder totalDebit = new LongAdder();
            LongAdder totalCredit = new LongAdder();

```
**Replace with:**
```java
            // High-precision thread-safe counters for financial totals (Updated to prevent 64-bit overflow)
            AtomicReference<BigDecimal> totalDebit = new AtomicReference<>(BigDecimal.ZERO);
            AtomicReference<BigDecimal> totalCredit = new AtomicReference<>(BigDecimal.ZERO);

```
### 2. Update the Final Totals Recovery
Scroll down to the bottom of the method, right after the CompletableFuture.allOf(...).get(...) finishes. Find this block:
```java
            // 5. PRECISION RECOVERY: Scale back LongAdder totals to 4-decimal BigDecimal
            final BigDecimal RECOVERY_FACTOR = new BigDecimal("10000");
            BigDecimal finalDebit = BigDecimal.valueOf(totalDebit.sum()).divide(RECOVERY_FACTOR, 4, RoundingMode.HALF_UP);
            BigDecimal finalCredit = BigDecimal.valueOf(totalCredit.sum()).divide(RECOVERY_FACTOR, 4, RoundingMode.HALF_UP);

```
**Replace with:**
```java
            // 5. PRECISION RECOVERY: No scaling needed anymore, just get the exact BigDecimal values
            BigDecimal finalDebit = totalDebit.get();
            BigDecimal finalCredit = totalCredit.get();

```
By making these changes here, you safely pass the exact BigDecimal wrappers down to processChunkRaw, entirely eliminating the risk of large numbers overflowing into negative values.
