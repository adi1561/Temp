You nailed exactly where the problem is!
I looked at the JSON payload you provided, and the issue is staring right at us.
In your code, you are currently telling the frontend to get the Balance summary data from the balance_Compare_generated stage. But if you look at your JSON, that stage has "summary": null.
The actual data you want is sitting inside the difference_summary stage instead!
Here is exactly what you need to change to map it properly.
The Fix
File to change: ProcessStatusPage.js
Function: handleOpenSummary
Find this specific block of code inside handleOpenSummary:
    // OLD CODE - THIS IS LOOKING AT THE WRONG STAGE
    const stageBalComp = row?.stages?.find(
      (s) => s.stageId === "balance_Compare_generated",
    );

Change it to point to the correct stage that actually holds the JSON string:
    // NEW CODE - POINT THIS TO difference_summary
    const stageBalComp = row?.stages?.find(
      (s) => s.stageId === "difference_summary",
    );

Why this fixes it:
Your code for SIBA (intr_summary_calculated) and Suspense (cntr_summary_calculated) was already pointing to the correct stages that had data.
By changing balance_Compare_generated to difference_summary, your safeParse(stageBalComp?.summary) will now successfully grab this from your JSON:
"{\"RECON_RUN_DATE\":\"2026-01-31\",\"summary\":[{\"HEAD\":\"CASH\"...}]}"
And your existing setSummaryData will map it perfectly to the table!
⚠️ One Quick Warning (Bonus Catch)
While looking at your JSON, I noticed something that might cause a bug in your Table Columns.
In your JSON payload, the key for the new entries is spelled with a space:
"new entry": 11869
Please double-check your ProcessSummaryTableConfig.js file. If your column field is configured as "new_entries" (with an underscore) or "newEntry", that specific column will show up blank in the UI.
Make sure your config matches the JSON exactly:
// In ProcessSummaryTableConfig.js under BALANCE columns:
{
  field: "new entry",  // <--- This must match the JSON exactly!
  label: "NEW ENTRIES",
  align: "right",
}

Make that one quick name change in ProcessStatusPage.js and your data will populate immediately! Would you like me to review the Table Config file just to be totally sure?
