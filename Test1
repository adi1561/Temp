@Cacheable          RedisTemplate
-----------         -------------
Automatic           Manual
Limited control     Full control
Only cache          Store anything
Simple              Flexible

from this i think it is better to use the RedisTemplate is it ?


if we are using the RedisTemplate then in that case we need to use Redies Manger to decide which serilization to use like 
        // Values stored as JSON
        template.setValueSerializer(
            new GenericJackson2JsonRedisSerializer());
        template.setHashValueSerializer(
            new GenericJackson2JsonRedisSerializer());


            if we are using this it means we are storing the data in the json is it 
            but it is like i see that it is stored in the form of string value 



 RedisTemplate<String, Object> template = new RedisTemplate<>();

 what ever this RedisTemplate return it is like we can just pass the String and object as key value only cant we store like string string or anything else 

 
