Based on the screenshot and details you provided, the Data Lake schema has changed significantly from what was originally mapped in your application.
Here is exactly what changed:
 1. **The Table Name:** It is now gl_balance instead of GL_BALANCE_HDFS.
 2. **The Balance Column:** It is now closing_balance instead of BALANCE.
 3. **The Matching Logic (WHERE Clause):** Instead of checking BRANCH_CODE, CURRENCY, and CGL separately, you now need to concatenate them into a single string to match the glcc column.
Because you properly separated your database models using Data Transfer Objects (HdfsSyncDto), you **only need to change one file**. The Oracle retrieval logic and your recovery services can stay exactly the same.
Here is the exact file and the replacement code.
### The File to Update
[cite_start]**com/fincore/JournalService/Service/HdfsSyncService.java** [cite: 474-475]
### The Code Replacement
**Find this existing code block inside the syncToDataLake method:**
```java
        try {
            String sql = "UPDATE GL_BALANCE_HDFS SET BALANCE = ?, INR_BALANCE = ? " +
                    "WHERE BRANCH_CODE = ? AND CURRENCY = ? AND CGL = ? AND BALANCE_DATE = ?";

            hiveJdbcTemplate.batchUpdate(sql, new BatchPreparedStatementSetter() {
                @Override
                public void setValues(PreparedStatement ps, int i) throws SQLException {
                    HdfsSyncDto dto = data.get(i);
                    ps.setBigDecimal(1, dto.getNewBalance());
                    ps.setBigDecimal(2, dto.getNewInrBalance());
                    ps.setString(3, dto.getBranch());
                    ps.setString(4, dto.getCurrency());
                    ps.setString(5, dto.getCgl());
                    ps.setDate(6, java.sql.Date.valueOf(dto.getBalanceDate()));
                }

                @Override
                public int getBatchSize() {
                    return data.size();
                }
            });

```
**Replace it entirely with this updated code:**
```java
        try {
            // Updated to match the new Data Lake schema
            String sql = "UPDATE gl_balance SET closing_balance = ?, INR_BALANCE = ? " +
                    "WHERE glcc = ? AND BALANCE_DATE = ?";

            hiveJdbcTemplate.batchUpdate(sql, new BatchPreparedStatementSetter() {
                @Override
                public void setValues(PreparedStatement ps, int i) throws SQLException {
                    HdfsSyncDto dto = data.get(i);
                    
                    // Dynamically construct the composite GLCC key
                    String glccKey = dto.getBranch() + dto.getCurrency() + dto.getCgl();

                    ps.setBigDecimal(1, dto.getNewBalance());    // Maps to closing_balance
                    ps.setBigDecimal(2, dto.getNewInrBalance()); // Maps to INR_BALANCE
                    ps.setString(3, glccKey);                    // Maps to glcc (e.g., 1000USD7002070601)
                    ps.setDate(4, java.sql.Date.valueOf(dto.getBalanceDate())); // Maps to BALANCE_DATE
                }

                @Override
                public int getBatchSize() {
                    return data.size();
                }
            });

```
