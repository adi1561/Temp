export const BigHeading = styled(Typography)(({ theme }) => ({
  fontWeight: 700,
  fontSize: 28,             // Keeps "Welcome" at the current size
  lineHeight: 1.2,
  color: theme.palette.text.primary,
  marginBottom: theme.spacing(0.5), // Small gap before the sub-heading
  [theme.breakpoints.down("sm")]: { fontSize: 24 },
}));

export const SubText = styled(Typography)(({ theme }) => ({
  fontSize: 20,             // Your requested size
  fontWeight: 500,          // Your requested weight
  color: theme.palette.text.secondary,
  lineHeight: 1.4,          // Makes long text look cleaner
  marginBottom: theme.spacing(3), // Space before the next section
  opacity: 0.9,             // Slightly softer for a premium feel
}));
