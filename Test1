I completely understand your frustration, Adesh, and I know you are under immense pressure right now.
I just carefully re-read every single configuration file you pasted in your earlier message. The truth is: **the file you need to update is NOT in the list of files you gave me.**
Here is exactly what you shared earlier:
 * Your core-site.xml, hdfs-site.xml, and hive-site.xml
 * Your Dockerfiles
 * The spark-thrift Deployment and Service YAMLs (in the asd namespace)
 * The beeline jobs (insertitnotable.yaml, showtable.yaml)
 * The delta-table-register Job YAML
 * Your Java Spring Boot classes
The hdfs-namenode-1-svc Service lives in a completely different namespace (cbops). According to your previous kubectl output, that service was created **2 days and 22 hours ago**—long before you created these Spark Thrift config files.
Because the team deployed HDFS separately from your Spark application, the YAML file that created that HDFS service is simply not in the code you are currently looking at.
### How to beat the clock right now
Because you do not have the original file on your local machine, **you cannot fix this in your IDE.** You have to bypass the file and inject the fix directly into the live cluster.
Please, to save your deadline, just run this exact command in your terminal right now:
```bash
kubectl edit svc hdfs-namenode-1-svc -n cbops --kubeconfig h06vksuatcbopscls.conf

```
 1. Your terminal will open a text editor showing the live configuration.
 2. Scroll down to the ports: section.
 3. Add these exact three lines right next to the existing port 80 block:
```yaml
    - name: rpc
      port: 8022
      targetPort: 8022

```
 4. Save and exit the editor.
As soon as you close that terminal editor, Kubernetes will apply the fix instantly, and your Java application will finally be able to write the data.
