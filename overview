I want to create a functionality to create some ASCII kind of files from the configuration mentioned in the db table and an input pipeseperated textfile.
 
I have attached the table and sample input file and its ASCII generated file .
 
Now table info-
 
Report	- nwsa
Type	- extension of the file to be generated
Input_file_name	- Name of input file
Output_First_Line	- the first line of ascii file output, it will compulsorily have whatever mentioned here along with 5 digit L paded Branch Code then reportDate
Output_End_line	- whatever mentioned here should be printed in the end line of ASCII file.
Input_Head_Col	- In which column the head is in input psv. Head is used to identify the row.
Input_Head_REGEX	- Regex to identify the head to be taken for further processing from the column mentioned in Input_Head_Col
Output_Head_Col_pad - The Head column is to be compulsorily present in whatever value is generating in ASCII with the padding mentioned in this column - this can have values like 5L0 this means padding to be applied 5 Left padding and padding of zeros if there is 6R" " this means padding to be applied 6 rignt padding of empty spaces.
Amount_col_SEQ	- sequence in which the values from amount column from input file to be printed. like 3 - this means, from input psv 3rd column to is of amount and should be printed after Head col. 5else6 means look in column 5 if there is value then print it other wise print amount from column 6, 5and1 - means in ascii from input psv 5 th column to be printed then 1 st column to be printed.
Output_Amt_Col_Logic - the logic to be applied to the amount column which to be printed in ascii, this is like [regex to be tested for head- means if the head is like this then only apply the operation on the amount column mentioned in next bracket][amount column on which the operation to be performed][operation to be performed like /1000 *-1]|second logic , the logics are pipe seperated. eg. ["^S\d{1,3}$|^NXY\d{2}$"][3][/(1000)]
Output_Amt_Decimal	- the value transformation to be applied like if same [head regex][amount col from psv][transformation logic] - transformation logic like some digit, floor, ceil, round, [""][3][2] means for the selected head what -> from 3 column -> there should be decimal value with 2 digits if not then apply .00, similarly for 4 , for round , floor, ceil do the necessary operations. there can be multiple operations applied to the same column or different column with | seperated.
Output_Amt_Col_pad - the padding to be applied to the amount column [head regex][amount col index][16,2]|[regex][4][18] means the amount column should be printed with 16 digits before . and 2 after . , then for other regex for col 4, apply 18 digits means if the input value is 125.32 then it will be like 000000000000012532
Output_Per_line_head - in ascii in each like how much such values to be printed like if Output_Per_line_head is 1 then only 1 head generated value to be printed in one line like - 0L01A0000000000000125.36
Output_Amt_Sign - [head regex][4][NO_SIGN]|[regex][3][*(-1)]|[regex][3][SHOW_SIGN] if no sign means what even sign is there it should not be printed. if SHOW_SIGN then sign to be printen for the value + also and - also, and if any other then perform the operation and show the sign. And remember sign is to be printed after amount like 0L01A0000000000000125.36- or 0L01A0000000000000125.36+
 
Create a Oracle sql table named ASCII_CONFIG with proper comments possible values and write code.
generate production grade, efficient, optimized, easily extendable, DRY, SOLID spring boot code.

this is my prvious promt according to this 

ID	NUMBER	No		1	
REPORT_ID	VARCHAR2(50 BYTE)	Yes		2	
FILE_TYPE	VARCHAR2(10 BYTE)	Yes		3	
INPUT_FILE_NAME	VARCHAR2(100 BYTE)	Yes		4	
OUTPUT_FIRST_LINE	VARCHAR2(100 BYTE)	Yes		5	
OUTPUT_END_LINE	VARCHAR2(100 BYTE)	Yes		6	
INPUT_HEAD_COL	NUMBER	Yes		7	
INPUT_HEAD_REGEX	VARCHAR2(200 BYTE)	Yes		8	
OUTPUT_HEAD_COL_PAD	VARCHAR2(50 BYTE)	Yes		9	
AMOUNT_COL_SEQ	VARCHAR2(50 BYTE)	Yes		10	
OUTPUT_AMT_COL_LOGIC	VARCHAR2(4000 BYTE)	Yes		11	
OUTPUT_AMT_DECIMAL	VARCHAR2(4000 BYTE)	Yes		12	
OUTPUT_AMT_COL_PAD	VARCHAR2(4000 BYTE)	Yes		13	
OUTPUT_AMT_SIGN	VARCHAR2(4000 BYTE)	Yes		14	
OUTPUT_PER_LINE_HEAD	NUMBER	Yes		15	
CREATED_DATE	TIMESTAMP(6)	Yes	"SYSDATE 
"	16	

this is my alredy table now according this i think in my table only regax there is problem 
now give mi cmd for inserting the new data line according the above discription and make sure u are adding the padding and all accordingly 
also give mi updated code for that 
