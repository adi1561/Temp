Here is the Controller code. It connects your API request to the Service we just built.
Controller (NwsaController.java)
Path: src/main/java/com/tcs/fincore/controller/NwsaController.java
package com.tcs.fincore.controller;

import com.tcs.fincore.service.GenericReportService;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.*;

@RestController
@RequestMapping("/api/reports")
public class NwsaController {

    @Autowired
    private GenericReportService reportService;

    // Endpoint: POST http://localhost:8080/api/reports/nwsa/generate?filePath=C:/YourData/nwsa_input.psv
    @PostMapping("/nwsa/generate")
    public ResponseEntity<String> generateNwsaReport(@RequestParam String filePath) {
        try {
            System.out.println("Received Request for NWSA Report...");
            System.out.println("Input File Path: " + filePath);

            // Trigger the service for 'nwsa' specifically
            String outputFilePath = reportService.generateReport("nwsa", filePath);
            
            return ResponseEntity.ok("✅ SUCCESS! Report Generated at: " + outputFilePath);

        } catch (Exception e) {
            e.printStackTrace();
            return ResponseEntity.internalServerError().body("❌ FAILED: " + e.getMessage());
        }
    }
}

How to trigger this
Method: POST
URL: http://localhost:8080/api/reports/nwsa/generate
Query Param: filePath = C:/Users/YourName/Documents/nwsa_input.psv (or your actual path)
Would you like me to write a curl command or Postman collection snippet to help you test this quickly?
