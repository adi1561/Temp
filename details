I understand exactly what is happening. **Both of these errors—even though they look different—are caused by the exact same SD environment encryption issue.**
Here is why they look different to you:
 1. **For the Checker Action (which you saw in the backend logs):** The backend tried to parse the encrypted string into ProcessVoucherRequestDto and threw the JSON_PARSE_ERROR stack trace you shared.
 2. **For the Manual Upload (which you saw on the frontend):** The exact same encryption crash happens on the backend, but your frontend catch (error) block in VoucherPosting.jsx hides the backend stack trace and shows the generic generic message: *"Unable to submit the voucher request. Please try again."*
To fix **both** issues, you need to bypass the payload encryption for all POST requests. Here are the exact lines you need to update in your frontend files.
### 1. Fix for Manual Upload (VoucherPosting.jsx)
Find the handleValidateClick function (around line 430) where the manual batch is submitted.
**Replace this:**
```javascript
    try {
      const response = await callApi("/JS/voucher/create-batch", finalPayload, "POST");
      const newBatchId = response?.[0]?.batchId || response?.batchId || "N/A";

```
**With this:**
```javascript
    try {
      // The 'false' at the end bypasses the SD encryption so the backend can read the JSON
      const response = await callApi("/JS/voucher/create-batch", finalPayload, "POST", "json", null, {}, false);
      const newBatchId = response?.[0]?.batchId || response?.batchId || "N/A";

```
### 2. Fix for Bulk Upload / Scheduling (VoucherPosting.jsx)
Find the handleFinalizeRouting function (around line 249) where the bulk and manual schedules are finalized.
**Replace these two API calls:**
```javascript
      if (entryMethod === "BULK") {
        // ...
        const response = await callApi("/JS/voucher/create-batch-from-cache", {
          requestId: submittedBatchId,
          postingDate: formattedBackendDate,
          vid: selectedVoucher.vid,
          commonBatchRemarks: "Voucher created successfully",
          scheduledTime: formattedSchedule
        }, "POST");
        // ...
      } else {
        await callApi("/JS/voucher/update-schedule", {
          batchId: submittedBatchId,
          scheduledTime: formattedSchedule
        }, "POST");
      }

```
**With this:**
```javascript
      if (entryMethod === "BULK") {
        // ...
        const response = await callApi("/JS/voucher/create-batch-from-cache", {
          requestId: submittedBatchId,
          postingDate: formattedBackendDate,
          vid: selectedVoucher.vid,
          commonBatchRemarks: "Voucher created successfully",
          scheduledTime: formattedSchedule
        }, "POST", "json", null, {}, false); // <--- Bypass Encryption
        // ...
      } else {
        await callApi("/JS/voucher/update-schedule", {
          batchId: submittedBatchId,
          scheduledTime: formattedSchedule
        }, "POST", "json", null, {}, false); // <--- Bypass Encryption
      }

```
### 3. Fix for Checker Action (VoucherAuthorization.jsx)
Find the handleSubmitAction function (around line 496).
**Replace this:**
```javascript
    try {
      const response = await callApi(
        "/JS/voucher/workflow/action",
        {
          batchId: viewingBatchId,
          status: statusToSubmit,
          remarks: executorRemarks.trim(),
          overrideScheduleLapse: overrideLapse,
          newScheduledTime: formattedNewSchedule,
        },
        "POST"
      );

```
**With this:**
```javascript
    try {
      const response = await callApi(
        "/JS/voucher/workflow/action",
        {
          batchId: viewingBatchId,
          status: statusToSubmit,
          remarks: executorRemarks.trim(),
          overrideScheduleLapse: overrideLapse,
          newScheduledTime: formattedNewSchedule,
        },
        "POST",
        "json",
        null,
        {},
        false // <--- Bypass Encryption
      );

```
Once you apply false as the 7th parameter to these callApi requests, your frontend will send standard, readable JSON to the server. This will instantly resolve the JSON_PARSE_ERROR in the backend and the "Unable to submit" error in the frontend!
