ok lets discuss then what is the runnable class how to use this and all
when we use 

class MyTask implements Runnable {
    public void run() {
        // This is where you put the code you want to run
        System.out.println("The task is being performed!");
    }
}

in this case it wrap the class with runnble object

MOST IMP :-  in every case it always convert into the runnble object when we use the thread 

now when we a class implements the Runnable vs when we are using the @async what is the diff

if we are doing like we use the @async on the top of the methods it ultimately convert into the Runnble only same like above 
internally it doing that only


why it convert into the Runble object always ??

beocase the thread is progreammed in a way that it will just find that object which is wrapped with the Runble object amd then
he will find the run methods and run the code

something like this it works

the key diff is like when we are using the @async for a particular method then not whole class gets convert into the Runnble object 
only that that methods is converted into the Runnble object that having that @async on the top


now how it know it uses the PROXY

lets understand this what is proxy and all how it works

it is like when we write a @async on the top of the method any method from that class it actually convert that class class to the 
proxy it means that it creating the duplicate class like or we can say as protection class or filter or bodygurad to that class 
why is it so

when we are calling the class which having a methods with @async first it will intersecpt pass this to the duplicate or proxy class
then it will cheak the methods which we call that methods is having the @async 

if yes -> it will intersecpt and it will grab that class create a thread and send it to the thread pool soo....

if no -> it send as it is to the original class 
