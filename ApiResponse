Here are the exact replacements to update **VoucherPosting.js** so it correctly reads the uppercase keys (ID, VID, CATEGORY_NAME, ISSUE_CATEGORIES, CREATOR_ID, CREATOR_NAME, CREATED_AT, DESCRIPTION) from your new API response.
### 1. Update the Main Table Row Mapping
Find your voucherList.slice(...).map((row) => { block inside the <TableBody> (around line 335) and replace the **entire return ( ... ) block** with this version that maps the new uppercase keys:
```javascript
                <TableBody>
                  {voucherList
                    .slice(page * rowsPerPage, page * rowsPerPage + rowsPerPage)
                    .map((row) => {
                      // Extracted explicitly using the new uppercase API keys
                      const rowId = row.ID;
                      const rowVid = row.VID;
                      const rowCategory = row.CATEGORY_NAME || "General";
                      const rowCreatorName = row.CREATOR_NAME || "System";
                      const rowCreatorId = row.CREATOR_ID || "N/A";
                      const rowCreatedAt = row.CREATED_AT;
                      const rowDescription = row.DESCRIPTION || "No description provided for this voucher configuration.";
                      const rowIssueCategories = row.ISSUE_CATEGORIES;

                      const isSelected = selectedVoucher?.ID === rowId;
                      const isExpanded = expandedRow === rowId;

                      return (
                        <React.Fragment key={rowId}>
                          {/* MAIN ROW */}
                          <TableRow
                            hover
                            onClick={() => {
                              setSelectedVoucher(row);
                              handleRowExpand(rowId);
                            }}
                            sx={{
                              cursor: 'pointer',
                              backgroundColor: isSelected ? "action.selected" : "inherit",
                              '& > *': { borderBottom: isExpanded ? 'none' : '1px solid rgba(224, 224, 224, 1)' }
                            }}
                          >
                            <TableCell padding="checkbox">
                              <Radio checked={isSelected} color="primary" />
                            </TableCell>
                            <TableCell sx={jpStyles.ellipsisCell}>
                              {rowVid}
                            </TableCell>
                            <TableCell sx={jpStyles.ellipsisCell}>
                              {rowCategory}
                            </TableCell>
                            <TableCell sx={jpStyles.ellipsisCell}>
                              {rowCreatorName} <Typography component="span" variant="caption" color="text.secondary">({rowCreatorId})</Typography>
                            </TableCell>
                            <TableCell sx={jpStyles.ellipsisCell}>
                              {rowCreatedAt ? (
                                new Date(rowCreatedAt).toLocaleDateString("en-IN", { day: "numeric", month: "short", year: "numeric" })
                                + ", " +
                                new Date(rowCreatedAt).toLocaleTimeString("en-IN", { hour: "numeric", minute: "2-digit", hour12: true }).toLowerCase()
                              ) : "N/A"}
                            </TableCell>
                          </TableRow>

                          <TableRow sx={{ backgroundColor: "background.default" }}>
                            <TableCell style={{ paddingBottom: 0, paddingTop: 0, borderBottom: isExpanded ? '1px solid rgba(224, 224, 224, 1)' : 'none' }} colSpan={5}>
                              <Collapse in={isExpanded} timeout="auto" unmountOnExit>
                                <Grid container sx={jpStyles.collapseGrid}>
                                  <Grid item size={{ lg: 1.3, md: 1.5 }}>
                                    <Typography variant="body2" color="text.secondary">
                                      <strong>Description:</strong>
                                    </Typography>
                                  </Grid>
                                  <Grid item size={{ lg: 10.7, md: 10.5 }}>
                                    <Typography variant="body2" color="text.secondary">
                                      {rowDescription}
                                    </Typography>
                                  </Grid>
                                  <Grid item size={{ lg: 1.3, md: 1.5 }}>
                                    <Typography variant="body2" color="text.secondary">
                                      <strong> Issue Categories: </strong>
                                    </Typography>
                                  </Grid>
                                 <Grid item size={{ lg: 10.7, md: 10.5 }}>
                                    <Box>
                                      {rowIssueCategories ? 
                                        rowIssueCategories.split(",")
                                          .map((cat, index) => (
                                            <Chip key={index} label={cat.trim()} sx={jpStyles.issueCategoryChip} />
                                          ))
                                        : 
                                        <Typography variant="body2" color="text.secondary">N/A</Typography>
                                      }
                                    </Box>
                                  </Grid>
                                </Grid>
                              </Collapse>
                            </TableCell>
                          </TableRow>
                        </React.Fragment>
                      );
                    })}
                  {voucherList.length === 0 && (
                    <TableRow>
                      <TableCell colSpan={5} align="center" sx={{ py: 3 }}>
                        No configurations found.
                      </TableCell>
                    </TableRow>
                  )}
                </TableBody>

```
### 2. Update the SelectedVoucherCard
Find the SelectedVoucherCard component definition (around line 265, right above if (isPageLoading)) and replace it with this version to map the uppercase keys:
```javascript
  const SelectedVoucherCard = () => {
    if (!selectedVoucher) return null;

    return (
      <Paper elevation={0} sx={jpStyles.svPaper}>
        <Box sx={jpStyles.svHeaderBox}>
          <Typography variant="subtitle1" fontWeight="bold" color="text.primary">
            Selected Voucher Details
          </Typography>

        </Box>

        <Grid container>
          <Grid item sx={{ mb: 2 }} size={12}>
            <Stack direction="row" spacing={10}>
              <Box>
                <Typography variant="caption" color="text.secondary" display="block" gutterBottom>VID</Typography>
                <Typography variant="body1" fontWeight="bold" color="primary">{selectedVoucher.VID}</Typography>
              </Box>
              <Box>
                <Typography variant="caption" color="text.secondary" display="block" gutterBottom>Category</Typography>
                <Typography variant="body1" fontWeight="500">{selectedVoucher.CATEGORY_NAME || "General"}</Typography>
              </Box>
            </Stack>
          </Grid>
          <Grid item xs={12}>
            <Typography variant="caption" color="text.secondary" display="block" gutterBottom>Description</Typography>
            <Typography variant="body2" sx={{ whiteSpace: "pre-wrap", lineHeight: 1.6, fontWeight: 300 }}>
              {selectedVoucher.DESCRIPTION || "No description provided."}
            </Typography>
          </Grid>
        </Grid>
      </Paper>
    );
  };

```
### 3. Update the Submit Payload
Find the finalPayload object inside your handleSubmit function (around line 240) and update the vid property to read from selectedVoucher.VID.
```javascript
    const finalPayload = {
      vid: selectedVoucher?.VID,
      scheduledTime: null, // Handled in Step 3 now
      commonBatchRemarks: commonBatchRemarks.trim(),
      rows: rowPayloads,
    };

```
