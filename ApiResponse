// Example of what to DELETE:
if (row.currency === "INR" || row.currency?.toUpperCase() === "INR") {
  const decimals = String(row.amount).split(".")[1];
  if (decimals && decimals.length > 2) {
    showSnackBar("INR amounts cannot have more than 2 decimal places", "error");
    return false; // Or throw error
  }
}
