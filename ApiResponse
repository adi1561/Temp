Here are the exact snippets to find and replace in your VoucherRequestServiceImpl.java.
### 1. Fix Master Table Nulls & Override Default (executeAsyncBatchCreation)
**FIND this block where you initialize the master record:**
```java
    VoucherPosting posting = new VoucherPosting();
    // ... existing fields ...
    posting.setExecutionScheduled(scheduledTime != null ? "YES" : "NO");
    posting.setScheduledTime(scheduledTime);
    posting.setIsScheduleOverride("YES"); // Or however it currently looks

```
**REPLACE with:**
```java
    VoucherPosting posting = new VoucherPosting();
    posting.setBatchId(batchId);
    posting.setVidFk(journalPrefix);
    
    // 1. Map missing fields
    posting.setCommonBatchRemarks(commonRemarks);
    posting.setCreatorRole(creatorRole);
    posting.setCreatedBy(creatorId);
    posting.setWorkflowCode(journalPrefix); // Map the workflow code
    
    // 2. Fix schedule and override initialization
    posting.setExecutionScheduled(scheduledTime != null ? "YES" : "NO");
    posting.setScheduledTime(scheduledTime);
    posting.setIsScheduleOverride("NO"); // MUST default to NO at creation
    posting.setBatchStatus("P");

```
### 2. Fix Workflow Scheduled Time Null (logWorkflowAction)
**FIND your existing SQL and update statement inside logWorkflowAction:**
```java
    String sql = """
        INSERT INTO VOUCHER_WORKFLOW (
            WORKFLOW_ID, BATCH_ID_FK, VID_FK, LAST_ACTION, 
            LAST_ACTION_BY, LAST_ACTION_AT, REMARKS
        ) VALUES (
            VOUCHER_WORKFLOW_SEQ.NEXTVAL, ?, ?, ?, ?, SYSTIMESTAMP, ?
        )
    """;
    jdbcTemplate.update(sql, batchId, vid, action, userId, remarks);

```
**REPLACE with:**
```java
    String sql = """
        INSERT INTO VOUCHER_WORKFLOW (
            WORKFLOW_ID, BATCH_ID_FK, VID_FK, LAST_ACTION, 
            LAST_ACTION_BY, LAST_ACTION_AT, REMARKS, SCHEDULED_TIME
        ) VALUES (
            VOUCHER_WORKFLOW_SEQ.NEXTVAL, ?, ?, ?, ?, SYSTIMESTAMP, ?, ?
        )
    """;
    // Pass scheduledTime at the end
    jdbcTemplate.update(sql, batchId, vid, action, userId, remarks, scheduledTime);

```
### 3. Fix the Override Logic (processWorkflowAction)
**FIND the scheduling block inside if (isFinalStep):**
```java
    boolean isOverride = Boolean.TRUE.equals(dto.getOverrideScheduleLapse());
    if (isOverride) {
        posting.setIsScheduleOverride("YES");
        posting.setExecutionScheduled(dto.getNewScheduledTime() != null ? "YES" : "NO");
        posting.setScheduledTime(dto.getNewScheduledTime());
    } 

```
**REPLACE with:**
```java
    // Check if the user is passing a brand new override time from the UI
    boolean isOverrideTriggered = Boolean.TRUE.equals(dto.getOverrideScheduleLapse()) || dto.getNewScheduledTime() != null;

    if (isOverrideTriggered) {
        posting.setIsScheduleOverride("YES"); 
        posting.setExecutionScheduled(dto.getNewScheduledTime() != null ? "YES" : "NO");
        posting.setScheduledTime(dto.getNewScheduledTime());
    } else {
        posting.setIsScheduleOverride("NO"); // Ensure it stays NO if not triggered
    }

```
### 4. Remove Data Redundancy (processChunkRaw or Bulk Insert Method)
**FIND your jdbcTemplate.batchUpdate SQL string for VOUCHER_POSTING_DATA:**
```sql
    INSERT INTO VOUCHER_POSTING_DATA (..., COMMON_BATCH_REMARKS, ...) VALUES (..., ?, ...)

```
**REPLACE by removing it entirely:**
 * Delete COMMON_BATCH_REMARKS from the SQL string.
 * Delete the mapping for commonRemarks in your BatchPreparedStatementSetter. (You only need REQ_NARRATION for the row-level remarks).
