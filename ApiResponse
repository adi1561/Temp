
showSnackBar(
        `Your request has been initiated. You will be notified once the batch is Created/Failed. Batch created successfully with Batch ID : ${newBatchId}`,
        "success"
      );
      setRows([{ ...initialRowState, id: Date.now() }]);
      setCommonBatchRemarks("");
      setChecked(false);
      setBranchInputValues({});
      setCglInputValues({});
    } catch (error) {
      // Safely grab the backend error message if available
      const errMsg = error?.response?.data?.message || error.message || "System error occurred.";
      showSnackBar(`Batch creation failed: ${errMsg}`, "error");
    } finally {
      setIsSubmitting(false);
    }
  };
  
  in this 
  
  ok there is one problem or we can say missunderstanding like why we are showing this msg Batch created successfully with Batch ID : ${newBatchId} here i want just this warning or something like that he will get the notification 
  
  then what ever the happing that should be using notification only  for Failed also and for the successfully also using notifiation only not using the snakbar
  
