Here are the exact step-by-step changes to align the helper texts to the left, update the search tooltips/hints to reflect that users can only search by Code/Number, and update the notifications for submission.
### 1. Update Branch Search Tooltips & Helper Text (Frontend)
Find the Autocomplete section for the **Branch** (around line 348). We are changing the tooltips, the "no options" text, and left-aligning the helper text.
**Find this block:**
```javascript
                    noOptionsText={
                      (branchInputValues[row.id] || "").length < 3 
                        ? "Type at least 3 characters..." 
                        : "Branch not found"
                    }
                    renderInput={(params) => (
                      <Tooltip title="Search by Branch name or Branch code" placement="top-start">

```
**Replace it with:**
```javascript
                    noOptionsText={
                      (branchInputValues[row.id] || "").length < 3 
                        ? "Type at least 3 characters of Branch Code..." 
                        : "Branch not found"
                    }
                    renderInput={(params) => (
                      <Tooltip title="Search by Branch Code" placement="top-start">

```
**Find the Branch Helper Text (around line 380):**
```javascript
                    <Typography variant="caption" sx={{ fontSize: "0.65rem", color: "text.secondary", mt: 0.5, display: "block", textAlign: "center" }}>
                      Min 3 characters required (alphabets, numbers)
                    </Typography>

```
**Replace it with:**
```javascript
                    <Typography variant="caption" sx={{ fontSize: "0.65rem", color: "text.secondary", mt: 0.5, ml: 1.2, display: "block", textAlign: "left" }}>
                      Min 3 characters required (Branch Code)
                    </Typography>

```
### 2. Update CGL Search Tooltips & Helper Text (Frontend)
Find the Autocomplete section for the **CGL** (around line 415).
**Find this block:**
```javascript
                    noOptionsText={
                      (cglInputValues[row.id] || "").length < 3 
                        ? "Type at least 3 characters..." 
                        : "CGL not found"
                    }
                    renderInput={(params) => (
                      <Tooltip title="Search by CGL or CGL Description" placement="top-start">

```
**Replace it with:**
```javascript
                    noOptionsText={
                      (cglInputValues[row.id] || "").length < 3 
                        ? "Type at least 3 digits of CGL Number..." 
                        : "CGL not found"
                    }
                    renderInput={(params) => (
                      <Tooltip title="Search by CGL Number" placement="top-start">

```
**Find the CGL Helper Text (around line 448):**
```javascript
                    <Typography variant="caption" sx={{ fontSize: "0.65rem", color: "text.secondary", mt: 0.5, display: "block", textAlign: "center" }}>
                      Min 3 characters required (alphabets, numbers)
                    </Typography>

```
**Replace it with:**
```javascript
                    <Typography variant="caption" sx={{ fontSize: "0.65rem", color: "text.secondary", mt: 0.5, ml: 1.2, display: "block", textAlign: "left" }}>
                      Min 3 digits required (CGL Number)
                    </Typography>

```
### 3. Left-Align the Remarks Helper Texts (Frontend)
**Find the Common Batch Remarks Helper Text (around line 320):**
```javascript
<FormHelperText sx={{ color: "text.secondary" }}>
  Allowed 2-30 chars (alphabets, numbers)
</FormHelperText>

```
**Replace it with:**
```javascript
<FormHelperText sx={{ color: "text.secondary", textAlign: "left", ml: 1.5 }}>
  Allowed 2-30 chars (alphabets, numbers)
</FormHelperText>

```
**Find the Individual Row Remarks Helper Text (around line 534):**
```javascript
<Typography variant="caption" sx={{ fontSize: "0.65rem", color: "text.secondary" }}>
  Allowed 2-30 chars (alphabets, numbers)
</Typography>

```
**Replace it with:**
```javascript
<Typography variant="caption" sx={{ fontSize: "0.65rem", color: "text.secondary", ml: 1.2, display: "block", textAlign: "left" }}>
  Allowed 2-30 chars (alphabets, numbers)
</Typography>

```
### 4. Update the Submit Success/Error Messages (Frontend)
Find your handleSubmit function where the callApi happens (around line 265). We will update the SnackBar messages exactly as requested.
**Find this block:**
```javascript
      showSnackBar(
        `Journal creation request submitted successfully with Batch ID: ${newBatchId}`,
        "success"
      );
      setRows([{ ...initialRowState, id: Date.now() }]);
      setCommonBatchRemarks("");
      setChecked(false);
      setBranchInputValues({});
      setCglInputValues({});
    } catch (error) {
      showSnackBar("Journal Batch submission failed", "error");
    } finally {

```
**Replace it with:**
```javascript
      showSnackBar(
        `Your request has been initiated. You will be notified once the batch is Created/Failed. Batch created successfully with Batch ID : ${newBatchId}`,
        "success"
      );
      setRows([{ ...initialRowState, id: Date.now() }]);
      setCommonBatchRemarks("");
      setChecked(false);
      setBranchInputValues({});
      setCglInputValues({});
    } catch (error) {
      // Safely grab the backend error message if available
      const errMsg = error?.response?.data?.message || error.message || "System error occurred.";
      showSnackBar(`Batch creation failed: ${errMsg}`, "error");
    } finally {

```
### 5. Update the Database Notification Text (Java Backend)
To ensure the Notification Bell in the UI shows the exact grammar you requested, update your JournalRequestServiceImpl.java file.
Open **JournalRequestServiceImpl.java**, scroll down to the createBatchRequest method (where manual batches are saved).
**Find this line near the end of the method:**
```java
        sendNotification(creatorId, null, "Manual Batch " + batchId + " created successfully.", batchId);

```
**Replace it with:**
```java
        sendNotification(creatorId, null, "Batch created Successfully with Batch ID : " + batchId, batchId);

```
*(Note: Because manual batches are processed synchronously, a "Failed" status will instantly throw an exception and block the batch from being created entirely, which is why we only need to send the "Success" notification to the database!)*
