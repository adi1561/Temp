Here are the exact changes for both your React Frontend and Spring Boot Backend to restrict the scheduling exclusively to **today's date**, up to **11:59 PM**, while preserving the minimum 30-minute buffer.
### Part 1: Frontend Changes (VoucherPosting.js)
We will replace the bulky DateTimePicker with a sleek TimePicker. The Date will automatically be locked to today under the hood.
**1. Update your imports:**
Change the import for DateTimePicker to TimePicker at the top of your file (around line 34):
```javascript
import { LocalizationProvider } from "@mui/x-date-pickers/LocalizationProvider";
import { AdapterDayjs } from "@mui/x-date-pickers/AdapterDayjs";
// REMOVE DateTimePicker and ADD TimePicker
import { TimePicker } from "@mui/x-date-pickers/TimePicker";

```
**2. Update the handleApplySchedule validation:**
Find the handleApplySchedule method (around line 180). We will add strict dayjs validations to ensure the user cannot bypass the limits before sending it to the backend. Replace it with this:
```javascript
  const handleApplySchedule = async () => {
    if (!scheduledTime || !scheduledTime.isValid()) {
      showSnackBar("Please select a valid time.", "warning");
      return;
    }

    // Force the date to strictly be TODAY, merging it with the selected time
    const today = dayjs();
    const finalSchedule = scheduledTime
      .year(today.year())
      .month(today.month())
      .date(today.date());

    const minAllowed = dayjs().add(28, 'minute'); // 2 min network buffer
    const maxAllowed = dayjs().endOf('day'); // 11:59:59 PM today

    if (finalSchedule.isBefore(minAllowed)) {
      showSnackBar("Schedule time must be at least 30 minutes from now.", "warning");
      return;
    }

    if (finalSchedule.isAfter(maxAllowed)) {
      showSnackBar("Schedule time cannot exceed today's end of day (11:59 PM).", "warning");
      return;
    }

    setIsApplyingSchedule(true);
    try {
      // Formats it cleanly for the Oracle Backend
      const formattedSchedule = finalSchedule.format('YYYY-MM-DD HH:mm:ss');

      await callApi("/JS/voucher/update-schedule", {
        batchId: submittedBatchId,
        scheduledTime: formattedSchedule
      }, "POST");

      showSnackBar("Schedule applied successfully!", "success");
      handleResetWizard();
    } catch (e) {
      showSnackBar(e.message, "error");
    } finally {
      setIsApplyingSchedule(false);
    }
  };

```
**3. Update the Picker UI component:**
Scroll down to **STEP 3** where the picker is rendered (around line 800) and replace the <DateTimePicker> block with this <TimePicker> block:
```javascript
                  <LocalizationProvider dateAdapter={AdapterDayjs}>
                    {/* Replaced DateTimePicker with TimePicker for a cleaner UX */}
                    <TimePicker
                      label="Select Execution Time (Today)"
                      value={scheduledTime}
                      onChange={(newValue) => setScheduledTime(newValue)}
                      minTime={dayjs().add(30, 'minute')}
                      maxTime={dayjs().endOf('day')}
                      timeSteps={{ minutes: 1 }}
                      slotProps={{
                        textField: {
                          size: 'small',
                          fullWidth: true,
                          sx: { mb: 3, caretColor: 'transparent' },
                          helperText: "Execution must be scheduled at least 30 mins from now, and before 11:59 PM today.",
                          onKeyDown: (e) => e.preventDefault(),
                          onPaste: (e) => e.preventDefault(),
                          inputProps: { readOnly: true }
                        }
                      }}
                    />
                  </LocalizationProvider>

```
### Part 2: Backend Changes (VoucherRequestServiceImpl.java)
We must update the backend validation so it rejects any request that attempts to schedule beyond 11:59 PM of the current day.
Find the validateScheduledTime method at the very bottom of your service class and replace it with this updated logic:
```java
    /**
     * Validates that the scheduled time is between 30 minutes from now
     * and 11:59:59 PM (End of Day) of the CURRENT date.
     */
    public void validateScheduledTime(LocalDateTime scheduledTime) {
        if (scheduledTime == null) return;

        LocalDateTime minTime = LocalDateTime.now().plusMinutes(28); // 2-minute network buffer
        
        // Sets the max time to strictly 23:59:59 of TODAY
        LocalDateTime maxTime = LocalDateTime.now().with(java.time.LocalTime.MAX);

        if (scheduledTime.isBefore(minTime)) {
            throw new CustomExceptions.InvalidDataException("Scheduled time must be at least 30 minutes from the current time.");
        }

        if (scheduledTime.isAfter(maxTime)) {
            throw new CustomExceptions.InvalidDataException("Scheduled time cannot exceed today's end of day (11:59 PM).");
        }
    }

```
