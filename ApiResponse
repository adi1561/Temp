I see exactly why the submit button is failing!
When you were deleting the Duplicate CGL logic in the previous step, you accidentally commented out the two lines that format the postingDate. Because those lines are commented out, the code crashes instantly when it tries to use formattedBackendDate at the bottom of the loop, which breaks the submit button.
Here is how to fix it in **JournalPosting.js**:
Find your handleSubmit function (around line 192).
**Find this block where the date is commented out:**
```javascript
    setIsSubmitting(true);   

    // const [day, month, year] = postingDate.split("-");
    // const formattedBackendDate = `${year}-${month}-${day}`;

    const branchCurrencyTotals = new Map();
    const branchEntryCheck = new Map();

```
**Replace it by uncommenting those two date lines, like this:**
```javascript
    setIsSubmitting(true);   

    // Uncomment these two lines so formattedBackendDate exists!
    const [day, month, year] = postingDate.split("-");
    const formattedBackendDate = `${year}-${month}-${day}`;

    const branchCurrencyTotals = new Map();
    const branchEntryCheck = new Map();

```
That's it! As soon as you uncomment those two lines, the formattedBackendDate variable will be available for the rows again, and your Submit button will work perfectly.
