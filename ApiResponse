Since you updated your backend API (/JS/voucher/active) with a RowMapper that perfectly converts the Oracle data into standard **camelCase** keys (like id, vid, categoryName, etc.), we need to remove the uppercase Oracle keys (row.VID, row.CATEGORY_NAME) from the frontend.
Additionally, we need to make sure your TimePicker in Step 3 automatically refreshes to a valid safe time (Current Time + 35 minutes) if the user sat on the manual entry screen for a long time before clicking "Schedule Execution".
Here are the **4 exact replacements** to make in your VoucherPosting.js file:
### 1. Revert the Table Row Mapping to camelCase
Find your voucherList.slice(...).map((row) => { block inside the <TableBody> (around line 320) and update the variable extraction to use the clean camelCase keys:
**Replace the variable declarations with this:**
```javascript
                    .map((row) => {
                      // FIX: Using clean camelCase keys exactly as mapped by the backend RowMapper
                      const rowId = row.id;
                      const rowVid = row.vid;
                      const rowCategory = row.categoryName || "General";
                      const rowCreatorName = row.createdByName || "System";
                      const rowCreatorId = row.createdBy || "N/A";
                      const rowCreatedAt = row.createdAt;
                      const rowDescription = row.description || "No description provided for this voucher configuration.";
                      const rowIssueCategories = row.issueCategories;

                      const isSelected = selectedVoucher?.id === rowId;
                      const isExpanded = expandedRow === rowId;

```
### 2. Revert the SelectedVoucherCard to camelCase
Find your SelectedVoucherCard component definition (around line 260) and update the UI to read the camelCase keys.
**Replace the <Grid container> section with this:**
```javascript
        <Grid container>
          <Grid item sx={{ mb: 2 }} size={12}>
            <Stack direction="row" spacing={10}>
              <Box>
                <Typography variant="caption" color="text.secondary" display="block" gutterBottom>VID</Typography>
                <Typography variant="body1" fontWeight="bold" color="primary">{selectedVoucher.vid}</Typography>
              </Box>
              <Box>
                <Typography variant="caption" color="text.secondary" display="block" gutterBottom>Category</Typography>
                <Typography variant="body1" fontWeight="500">{selectedVoucher.categoryName || "General"}</Typography>
              </Box>
            </Stack>
          </Grid>
          <Grid item xs={12}>
            <Typography variant="caption" color="text.secondary" display="block" gutterBottom>Description</Typography>
            <Typography variant="body2" sx={{ whiteSpace: "pre-wrap", lineHeight: 1.6, fontWeight: 300 }}>
              {selectedVoucher.description || "No description provided."}
            </Typography>
          </Grid>
        </Grid>

```
### 3. Update the Submit Payload vid
Find the finalPayload object inside your handleSubmit function (around line 225) and change it back to selectedVoucher.vid.
**Replace it with this:**
```javascript
    const finalPayload = {
      vid: selectedVoucher?.vid,
      scheduledTime: null, // Handled in Step 3 now
      commonBatchRemarks: commonBatchRemarks.trim(),
      rows: rowPayloads,
    };

```
### 4. Auto-Refresh the TimePicker in Step 3
If the user spends 15 minutes typing in manual rows and *then* clicks "Schedule Execution", the initial time stored in the state might be invalid (in the past). We need to refresh it dynamically.
Scroll down to Step 3 (around line 810) and find the "Schedule Execution" button.
**Replace the button with this:**
```javascript
                  <Button 
                    variant="contained" 
                    size="large" 
                    startIcon={<ScheduleIcon />} 
                    onClick={() => {
                      // FIX: Generate a fresh, valid time (+35 mins) exactly when they click the button
                      setScheduledTime(dayjs().add(35, 'minute').second(0).millisecond(0));
                      setShowSchedulePicker(true);
                    }} 
                    sx={jpStyles.largeBtn}
                  >
                    Schedule Execution
                  </Button>

```
That's it! Your VoucherPosting.js is now completely detached from the external API, running smoothly on your own backend, and the UI will automatically handle all scheduling edge cases perfectly.
