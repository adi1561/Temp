I see exactly what you mean. Your Branch, Currency, CGL, and Type fields have InputLabelProps={{ shrink: true }} applied to them, which forces the label to stay resting on the top border of the input box permanently.
However, Amount, Product Code, and Remarks are missing that property, so their labels fall down into the middle of the empty text box until you click on them.
Here are the exact replacements to fix those three fields so they perfectly match the rest of your form.
### 1. Update the Amount Field
Find the TextField for **Amount** (around line 430) and add the InputLabelProps.
**Find this:**
```javascript
                    <TextField
                    label="Amount *"
                    type="text"
                    value={row.amount}

```
**Replace with:**
```javascript
                    <TextField
                    label="Amount *"
                    type="text"
                    value={row.amount}
                    InputLabelProps={{ shrink: true }}

```
### 2. Update the Product Code Field
Find the TextField for **Product Code** (around line 490) and add the InputLabelProps.
**Find this:**
```javascript
                    <TextField

                      label="Product Code"
                      value={row.productCode}

```
**Replace with:**
```javascript
                    <TextField

                      label="Product Code"
                      value={row.productCode}
                      InputLabelProps={{ shrink: true }}

```
### 3. Update the Remarks Field
Find the TextField for **Remarks** (around line 515) and add the InputLabelProps.
**Find this:**
```javascript
                    <TextField
                      label="Remarks *"
                      value={row.remarks}
                      onChange={(e) => {

```
**Replace with:**
```javascript
                    <TextField
                      label="Remarks *"
                      value={row.remarks}
                      InputLabelProps={{ shrink: true }}
                      onChange={(e) => {

```
By adding InputLabelProps={{ shrink: true }} to these three fields, all of your column headers will sit perfectly aligned at the top of their respective boxes!
