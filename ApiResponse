Based on the full backend code you provided earlier, **no, there is no condition blocking zero amounts for manual uploads in your current backend.**
In your BatchRequestDto, the amount is only marked as @NotNull, which means it will happily accept 0 or 0.00.
If you look at your createBatchRequest method in JournalRequestServiceImpl.java (around line 856), it only checks for the INR decimal limit, but it does **not** check for zero:
```java
            // Existing code in your file
            BigDecimal amount = row.getAmount();

            // Strict Cross-Field Validation for INR
            if ("INR".equalsIgnoreCase(row.getCurrency()) && amount.stripTrailingZeros().scale() > 2) {
                throw new CustomExceptions.InvalidDataException("INR amounts cannot have more than 2 decimal places.");
            }

            entity.setAmount(amount); // <-- It saves 0 directly to the database here!

```
### How to fix it:
To protect the backend, you must manually add the zero-check right before entity.setAmount(amount);.
**Replace that section with this:**
```java
            BigDecimal amount = row.getAmount();

            // 1. Strict Cross-Field Validation for INR
            if ("INR".equalsIgnoreCase(row.getCurrency()) && amount.stripTrailingZeros().scale() > 2) {
                throw new CustomExceptions.InvalidDataException("INR amounts cannot have more than 2 decimal places.");
            }

            // 2. ADD THIS NEW CONDITION TO BLOCK ZERO
            if (amount.compareTo(BigDecimal.ZERO) == 0) {
                throw new CustomExceptions.InvalidDataException("Transaction amount cannot be zero.");
            }

            entity.setAmount(amount);

```
Once you add that amount.compareTo(BigDecimal.ZERO) == 0 check, your manual upload backend will be 100% protected against zero amounts!
