You are absolutely right, sharp eye! We need to make sure the useEffect is actually calling your internal /JS/voucher/active API and securely extracting the data payload we just structured in the backend.
Here is the exact update for your useEffect block in **VoucherPosting.js**:
Find your fetchInitialData function inside the useEffect (around line 195) and replace the voucher fetching portion with this:
```javascript
      try {
        setIsPageLoading(true);

        const roleToFetch = currentRoleId || 9001;
        
        // FIX: Using your internal API that now maps exact camelCase keys & issueCategories!
        const vouchersResponse = await callApi(`/JS/voucher/active?roleId=${roleToFetch}`, null, "GET");

        // Defensively extract the array whether your callApi unwraps the { data: [...] } or not
        if (vouchersResponse && Array.isArray(vouchersResponse.data)) {
          setVoucherList(vouchersResponse.data);
        } else if (Array.isArray(vouchersResponse)) {
          setVoucherList(vouchersResponse);
        } else {
          setVoucherList([]); // Safe fallback
        }

        // 1. FIRST: Check the EOD Status from your new API...

```
This perfectly connects your frontend back to your own service, handles the new JSON wrapper safely, and drops the dependency on the external /JE/ service completely!
