To apply the exact same scheduling and workflow routing logic to the Bulk Upload process, we need to inject the VoucherPosting and VoucherWorkflow creation logic into your async bulk execution method.
Right now, your manual entry (createBatchRequest) initializes the workflow, but your bulk entry (executeAsyncBatchCreation) stops after creating the JOURNAL_BATCH_MASTER record.
Here are the **3 exact backend changes** required.
### 1. Update your Controller DTO
The payload coming from the frontend for /JS/voucher/create-batch-from-cache needs to accept the new fields. Ensure your Java DTO class (e.g., CacheBatchSubmitDto) has these fields added:
```java
private LocalDateTime scheduledTime;
private String vid;

```
### 2. Update the Method Signatures
In VoucherRequestService.java (Interface) and VoucherRequestServiceImpl.java (Implementation), add scheduledTime and vid to the method signatures.
**Update createBatchFromCacheAsync:**
```java
    @Override
    @Transactional
    public String createBatchFromCacheAsync(String requestId, String commonRemarks, String creatorId, Integer creatorRole, LocalDateTime scheduledTime, String vid) {
        // ... (keep existing lock logic)

        progressService.sendProgress(batchId, creatorId, 0, "PROCESSING", "Initializing Batch Creation...");
        
        // Pass the new variables down!
        self.executeAsyncBatchCreation(batchId, journalPrefix, requestId, commonRemarks, creatorId, creatorRole, scheduledTime, vid);

        return batchId;
    }

```
**Update executeAsyncBatchCreation:**
```java
    @Override
    @Async("bulkExecutor")
    public void executeAsyncBatchCreation(String batchId, String journalPrefix, String requestId, String commonRemarks, String creatorId, Integer creatorRole, LocalDateTime scheduledTime, String vid) {
        // ...

```
### 3. Inject Workflow & Scheduling Logic into the Async Loop
Inside executeAsyncBatchCreation, right after it saves the summary to JOURNAL_BATCH_MASTER, we need to initialize the workflow exactly like we did for manual entry.
**FIND this code inside executeAsyncBatchCreation (around line 49):**
```java
            jdbcTemplate.update(sumSql, batchId, creatorId, ts, commonRemarks, actualCommittedRows,
                    finalDebit, finalCredit, batchStatus, null);

            // Audit and Notification Logic (Preserved Business Logic)
            if (actualCommittedRows != totalRows) {
                progressService.sendProgress(batchId, creatorId, 0, "FAILED", "Integrity Failure: Row count mismatch.");
            } else  {
                logAudit(creatorId, "BATCH_ASYNC", "ADD", "Successfully created batch " + batchId);

```
**REPLACE THAT ENTIRE else BLOCK WITH THIS:**
```java
            } else  {
                logAudit(creatorId, "BATCH_ASYNC", "ADD", "Successfully created batch " + batchId);

                // ==========================================
                // DYNAMIC WORKFLOW & SCHEDULING INITIALIZATION
                // ==========================================
                String workflowCode = jdbcTemplate.queryForObject(
                        "SELECT WORKFLOW_CODE FROM WORKFLOW_TRANSITIONS WHERE ALLOWED_ROLE_ID = ? AND WORKFLOW_ORDER = 1 FETCH FIRST 1 ROWS ONLY",
                        String.class, creatorRole
                );

                Long firstApproverWorkflowId = jdbcTemplate.queryForObject(
                        "SELECT WORKFLOW_ID FROM WORKFLOW_TRANSITIONS WHERE WORKFLOW_CODE = ? AND WORKFLOW_ORDER = 2",
                        Long.class, workflowCode
                );

                VoucherPosting posting = new VoucherPosting();
                posting.setBatchId(batchId);
                posting.setVidFk(vid);
                posting.setCreatedBy(creatorId);
                posting.setCreatorsRole(String.valueOf(creatorRole));
                posting.setPendingAt(firstApproverWorkflowId);
                posting.setBatchStatus("P"); 

                if (scheduledTime != null) {
                    posting.setExecutionScheduled("YES");
                    posting.setScheduledTime(scheduledTime);
                    posting.setIsScheduleOverride("NO");
                } else {
                    posting.setExecutionScheduled("NO");
                }

                voucherPostingRepository.save(posting);

                if (vid != null && !vid.isEmpty()) {
                    updateVidStatus(vid, VoucherStatus.USED);
                }

                logWorkflowAction(batchId, vid, "Submitted", creatorId, commonRemarks, scheduledTime);

                // --- End Workflow Logic ---

                progressService.sendProgress(batchId, creatorId, 100, "COMPLETED", "Batch Created Successfully");
                sendNotification(creatorId, null, "Batch created Successfully with Batch ID : " + batchId, batchId);

                NotificationConfigDto config = permissionConfigService.getConfig("JOURNAL_AUTH");
                if (config != null) {
                    String msg = String.format("New Batch %s by %s requires approval.", batchId, creatorId);
                    sendNotification(null, config.getTargetRoles(), msg, batchId);
                }
            }

```
Make these backend changes and update your Controller to accept the two new fields. Once this is running, let me know and we will easily wire up the React frontend to pass the scheduling data!
