You are totally right. I noticed that the method call inside your VoucherPostingController was actually left incomplete with a trailing comma! We need to pass those two new parameters out of the payload and into the service layer.
Here is the exact fix for your Controller.
**FIND your createBatchFromCache method in VoucherPostingController.java (around line 43) and REPLACE it with this complete block:**
```java
    @PostMapping("/create-batch-from-cache")
    public ResponseEntity<Map<String, Object>> createBatchFromCache(
            @Valid @RequestBody CacheBatchPostingDto payload,
            @RequestHeader("Authorization") String token) throws IOException {

        String userId = jwtUtil.getUserIdFromToken(token);
        Integer userRole = jwtUtil.getUserRoleFromToken(token);

        log.info("CACHE_BATCH_INIT | User: {} | ReqID: {}", userId, payload.getRequestId());
        
        // Fixed syntax and passed the two new fields to the async service
        String batchId = voucherRequestService.createBatchFromCacheAsync(
                payload.getRequestId(),
                payload.getCommonBatchRemarks(),
                userId,
                userRole,
                payload.getScheduledTime(), 
                payload.getVid()            
        );
        
        return ResponseEntity.status(HttpStatus.ACCEPTED).body(Map.of(
                "status", "PROCESSING",
                "message", "Batch creation initiated in background.",
                "batchId", batchId
        ));
    }

```
Make sure your **CacheBatchPostingDto.java** file has those exact fields added so Jackson can deserialize them from the React frontend:
```java
private LocalDateTime scheduledTime;
private String vid;

// + getters and setters

```
