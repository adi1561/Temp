I completely understand. We will discard the JournalPostingWizard.jsx idea entirely.
Instead, we will keep your existing JournalPosting.jsx file exactly as it is, and simply **inject the JournalBulkUpload file into Step 2** whenever the user clicks the "Excel Upload" button.
Here are the exact Find & Replace steps to connect them seamlessly.
### PART 1: Changes in JournalPosting.jsx
**1. Add the Import and State Variable**
At the top of your JournalPosting.jsx file, import the new bulk upload component and add a state to track the entry method.
**FIND THIS (around line 60):**
```javascript
  // --- INTERNAL WIZARD STATE ---
  const [activeStep, setActiveStep] = useState(0);

```
**REPLACE WITH:**
```javascript
  import JournalBulkUpload from "./JournalBulkUpload"; // Import the separate file

  // --- INTERNAL WIZARD STATE ---
  const [activeStep, setActiveStep] = useState(0);
  const [entryMethod, setEntryMethod] = useState("MANUAL"); // Tracks Manual vs Bulk

```
**2. Update the Method Selection & Add Reset Function**
Update the button click logic so it actually changes the state instead of showing the "coming soon" message, and add a reset function so the child component can restart the flow.
**FIND THIS:**
```javascript
  const handleMethodSelection = (method) => {
    if (method === "BULK") {
      showSnackBar("Bulk Upload flow coming soon!", "info");
    } else {
      handleNext();
    }
  };

```
**REPLACE WITH:**
```javascript
  const handleMethodSelection = (method) => {
    setEntryMethod(method);
    handleNext();
  };

  const handleResetWizard = () => {
    setActiveStep(0);
    setSelectedVoucher(null);
    setEntryMethod("MANUAL");
    setRows([{ ...initialRowState, id: Date.now() }]);
    setCommonBatchRemarks("");
    setScheduledTime("");
    setChecked(false);
  };

```
**3. Conditionally Render Manual vs Bulk in Step 2**
Wrap your existing manual grid so it only shows if MANUAL is selected, and add the <JournalBulkUpload> component right below it for when BULK is selected.
**FIND THIS (At the start of Step 2):**
```javascript
      {/* INTERNAL STEP 2: DATA ENTRY GRID */}
        {activeStep === 2 && (
        <Box>

```
**REPLACE WITH:**
```javascript
      {/* INTERNAL STEP 2: MANUAL DATA ENTRY GRID */}
      {activeStep === 2 && entryMethod === "MANUAL" && (
        <Box>

```
**FIND THIS (At the very bottom of the file, closing the components):**
```javascript
                </Button>
              </Box>
            </Box>
          </Box>
        </Box>
      )}
    </Box>
  );
}

```
**REPLACE WITH:**
```javascript
                </Button>
              </Box>
            </Box>
          </Box>
        </Box>
      )}

      {/* EXTERNAL STEP 2: BULK UPLOAD EXCEL COMPONENT */}
      {activeStep === 2 && entryMethod === "BULK" && (
        <JournalBulkUpload 
            selectedVoucher={selectedVoucher} 
            onBack={handleBack} 
            onReset={handleResetWizard} 
        />
      )}

    </Box>
  );
}

```
### PART 2: Changes in JournalBulkUpload.jsx
Now, open your separate JournalBulkUpload.jsx file so it accepts the props from JournalPosting.jsx and displays the Selected Voucher properly.
**1. Update the Component Signature**
**FIND THIS (around line 96):**
```javascript
const JournalBulkUpload = () => {
  const { callApi } = useApi();

```
**REPLACE WITH:**
```javascript
const JournalBulkUpload = ({ selectedVoucher, onBack, onReset }) => {
  const { callApi } = useApi();

```
**2. Link the Back and Reset Buttons**
**FIND THIS (around lines 250-270):**
```javascript
 const resetAll = () => {
    setActiveStep(0);
    setRequestId(null);
    setResultStatus(null);
    // ... [other reset states]
    setPage(0);
    setDownloadMsg("Downloading error report...");
    clearInput();
  };

  const handleBack = () => {
    setActiveStep(0);
    setResultStatus(null);
    setValidationStage(0);
    setResultMsg("");
    setIsProcessing(false);
    setAutoDlTriggered(false);
    setDownloadMsg("Downloading error report...");
  };

```
**REPLACE WITH:**
```javascript
  const resetAll = () => {
    setRequestId(null);
    setResultStatus(null);
    setValidationStage(0);
    setResultMsg("");
    setPreviewRows([]);
    setTotalRows(0);
    setIsProcessing(false);
    setAutoDlTriggered(false);
    setIsCsvFile(false);
    setIsErrorDownloaded(false);
    setBatchCreated(false); 
    setFile(null);
    setPage(0);
    setDownloadMsg("Downloading error report...");
    clearInput();
    if (onReset) onReset(); // Tells JournalPosting.jsx to go back to Step 0
  };

  const handleBackNavigation = () => {
    if (onBack) onBack(); // Tells JournalPosting.jsx to go back to Step 1
  };

```
**3. Add the Selected Voucher Card to the Top & Use handleBackNavigation**
**FIND THIS (At the start of the return statement around line 375):**
```javascript
  // --- ORIGINAL UI RENDER (Cleaned up without activeSteps) ---
  return (
    <Box sx={mainContainerStyle}>
      <Paper elevation={1} sx={controlPanelStyle}>

```
**REPLACE WITH:**
```javascript
  return (
    <Box sx={mainContainerStyle}>
      {/* --- STANDARDIZED HORIZONTAL SELECTION HEADER --- */}
      {selectedVoucher && (
        <Paper elevation={0} sx={{ p: 3, mb: 4, backgroundColor: "#f8fafc", borderRadius: 2, border: "1px solid", borderColor: "divider" }}>
          <Box sx={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', borderBottom: "1px solid", borderColor: "divider", pb: 1, mb: 2 }}>
            <Typography variant="subtitle1" fontWeight="bold" color="text.primary">
              Selected Voucher Details
            </Typography>
            <Button startIcon={<ArrowBackIcon />} onClick={handleBackNavigation} variant="outlined" size="small">
              Change Method
            </Button>
          </Box>
          
          <Grid container spacing={3}>
            <Grid item xs={12} sm={6}>
              <Typography variant="caption" color="text.secondary" display="block" gutterBottom>VID</Typography>
              <Typography variant="body1" fontWeight="bold" color="primary">{selectedVoucher.VID || selectedVoucher.vid}</Typography>
            </Grid>
            <Grid item xs={12} sm={6}>
              <Typography variant="caption" color="text.secondary" display="block" gutterBottom>Category</Typography>
              <Typography variant="body1" fontWeight="500">{selectedVoucher.CATEGORY_NAME || selectedVoucher.category || "General"}</Typography>
            </Grid>
            <Grid item xs={12}>
              <Typography variant="caption" color="text.secondary" display="block" gutterBottom>Description</Typography>
              <Box sx={{ backgroundColor: "white", p: 2, borderRadius: 1, border: "1px solid", borderColor: "divider" }}>
                <Typography variant="body2" color="text.primary" sx={{ whiteSpace: "pre-wrap", lineHeight: 1.6 }}>
                  {selectedVoucher.DESCRIPTION || selectedVoucher.description || "No description provided."}
                </Typography>
              </Box>
            </Grid>
          </Grid>
        </Paper>
      )}

      <Paper elevation={1} sx={controlPanelStyle}>

```
That's it! Now JournalPosting.jsx handles the Step 1/2/3 logic natively just like you wanted, and smoothly swaps out the bottom half for JournalBulkUpload when Excel is clicked.
