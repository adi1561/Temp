I can see exactly why the modal is not opening!
There are two issues in your code:
 1. **The Eye Button Crash:** Inside your handleOpenViewModal function, you are still calling setDetailPage(0) and setDetailRowsPerPage(10). But because we deleted the table and those state variables in the last step, React is throwing a hidden error (ReferenceError: setDetailPage is not defined) which crashes the app before it can open the modal.
 2. **Accidentally Deleted Code:** When you pasted the *Schedule Lapsed* modal earlier, you accidentally deleted the code for the main *Accept/Reject* modal! If you click the Checkmark or X, nothing will happen right now.
Let's fix both of these instantly. Here are the two blocks to replace.
### Step 1: Fix the handleOpenViewModal function
We will remove the dead table pagination state setters and properly set the workflowState so the Stepper knows which step to highlight.
**FIND THIS FUNCTION (Around line 324):**
```javascript
  const handleOpenViewModal = (row) => {
    setViewingBatch(row); // Store the row for the Live Stepper
    setViewingBatchId(row.batchId);
    setViewingBatchCreatorId(String(row.creatorId));
    setDetailPage(0);
    setDetailRowsPerPage(10);
    setIsViewModalOpen(true);
  };

```
**REPLACE WITH THIS:**
```javascript
  const handleOpenViewModal = (row) => {
    setViewingBatch(row); 
    setViewingBatchId(row.batchId);
    setViewingBatchCreatorId(String(row.creatorId));
    
    // Pass the workflow state so the Stepper highlights correctly!
    setViewingBatchState(row.workflowState || "PENDING_L1"); 
    
    setIsViewModalOpen(true);
  };

```
*(You can also completely delete the handleDetailPageChange and handleDetailRowsPerPageChange functions right below it, as they are no longer needed!)*
### Step 2: Restore the Missing Accept/Reject Dialog
Scroll down to the bottom where the UI renders. You'll notice the main Dialog is missing.
**FIND THIS EXACT SPOT (Around line 612):**
```javascript
      {/* --- Main Grid Approve/Reject Dialog --- */}

{/* --- Schedule Lapsed Edge Case Modal --- */}
      <Dialog 
        open={isScheduleLapseModalOpen} 

```
**REPLACE WITH THIS (This restores your Accept/Reject popup):**
```javascript
      {/* --- Main Grid Approve/Reject Dialog --- */}
      <Dialog 
        open={isModalOpen}
        onClose={handleCloseModal}
        fullWidth
        maxWidth="sm"
      >
        <DialogTitle>
          {modalMode === "ACCEPTED" ? "Accept" : "Reject"} Batch ID
        </DialogTitle>
        <DialogContent>
          <DialogContentText>
            Please provide the remarks to {modalMode === "ACCEPTED" ? "Accept" : "Reject"} Batch ID{" "}
            <strong>{selectedBatch?.batchId}</strong>
          </DialogContentText>
          <TextField
            autoFocus
            required
            margin="dense"
            label="Remarks"
            fullWidth
            variant="standard"
            value={executorRemarks}
            onChange={(e) => handleRemarksChange(e, setExecutorRemarks)}
            disabled={isSubmitting}
            inputProps={{ maxLength: 30 }}
            error={executorRemarks.length > 0 && !isRemarksValid(executorRemarks)}
          />
          <Typography sx={styles.helperText}>
            Allowed 2-30 chars (alphabets, numbers)
          </Typography>
        </DialogContent>
        <DialogActions sx={styles.dialogActions}>
          <Button onClick={handleCloseModal} disabled={isSubmitting}>
            Cancel
          </Button>
          <Button
            onClick={() => handleSubmitAction(false)}
            variant="contained"
            color={modalMode === "ACCEPTED" ? "success" : "error"}
            disabled={isSubmitting || !isRemarksValid(executorRemarks)}
          >
            {isSubmitting ? <CircularProgress size={24} /> : (modalMode === "ACCEPTED" ? "Accept" : "Reject")}
          </Button>
        </DialogActions>
      </Dialog>

      {/* --- Schedule Lapsed Edge Case Modal --- */}
      <Dialog 
        open={isScheduleLapseModalOpen} 

```
Save your file and click the eye button now! The detailed stepper should open instantly with a buttery smooth animation, and the Accept/Reject buttons will work perfectly again.
