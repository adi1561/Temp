Here are the exact replacements to fix those errors.
**1. Add the missing import at the top of the file**
You are missing the import for ParsingResult, which causes the Incompatible types, rows, and hasProductColumn errors.
Add this line to your imports at the top of VoucherBulkValidationService.java:
```java
import com.fincore.VoucherService.Dto.ParsingResult;

```
**2. Fix the updateState call in the processAsync method**
Around line 183, the code is trying to call updateState directly, but that method was moved to the stateManager.
**Find this exact line:**
```java
updateState(requestId, s -> s.setMessage("Parsing File..."));

```
**Replace it with:**
```java
stateManager.updateState(requestId, s -> s.setMessage("Parsing File..."));

```
**3. Delete the leftover padBranch method**
At the very bottom of your file, there is a leftover padBranch method. Delete these lines completely, as they are no longer needed here:
```java
    private String padBranch(String branch) {
        if (branch == null || branch.trim().isEmpty()) return "";
        String b = branch.trim();
        if (b.length() < 5) {
            return String.format("%5s", b).replace(' ', '0');
        }
        return b;
    }

```
