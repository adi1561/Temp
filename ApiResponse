Here are the exact step-by-step replacements to make all these changes in your React frontend and your Java backend.
### 1. Update Dialog Titles, Texts, and Buttons (Frontend)
Find the <Dialog> block for the Approve/Reject modal (around line 430).
**Find this block:**
```javascript
        <DialogTitle>
          {modalMode === "ACCEPTED" ? "Approve" : "Reject"} Batch
        </DialogTitle>
        <DialogContent>
          <DialogContentText>
            Confirm {modalMode.toLowerCase()} for batch{" "}
            <strong>{selectedBatch?.batchId}</strong>?
          </DialogContentText>

```
**Replace it with:**
```javascript
        <DialogTitle>
          {modalMode === "ACCEPTED" ? "Accept" : "Reject"} Batch ID
        </DialogTitle>
        <DialogContent>
          <DialogContentText>
            Please provide the remarks to {modalMode === "ACCEPTED" ? "Accept" : "Reject"} Batch ID{" "}
            <strong>{selectedBatch?.batchId}</strong>
          </DialogContentText>

```
**Find the Confirm Button (around line 457):**
```javascript
          <Button
            onClick={handleSubmitAction}
            variant="contained"
            color={modalMode === "ACCEPTED" ? "success" : "error"}
            disabled={isSubmitting || !isRemarksValid(executorRemarks)}
          >
            {isSubmitting ? <CircularProgress size={24} /> : "Confirm"}
          </Button>

```
**Replace it with:**
```javascript
          <Button
            onClick={handleSubmitAction}
            variant="contained"
            color={modalMode === "ACCEPTED" ? "success" : "error"}
            disabled={isSubmitting || !isRemarksValid(executorRemarks)}
          >
            {isSubmitting ? <CircularProgress size={24} /> : (modalMode === "ACCEPTED" ? "Accept" : "Reject")}
          </Button>

```
### 2. Update Remarks Regex to Allow Only Alphabets/Numbers (Frontend)
Find the filterAndWarn function (around line 170).
**Find this line:**
```javascript
      // 1. Strip any character that is NOT alphanumeric or in the allowed special chars list
      filtered = val.replace(/[^a-zA-Z0-9\s/&(),.-]/g, "");

```
**Replace it with:**
```javascript
      // 1. Strip any character that is NOT alphanumeric or space
      filtered = val.replace(/[^a-zA-Z0-9 ]/g, "");

```
**Find the Helper Text (around line 448):**
```javascript
          <Typography sx={styles.helperText}>
            Allowed 2-30 chars (alphanumeric, single spaces, / & ( ) , . -)
          </Typography>

```
**Replace it with:**
```javascript
          <Typography sx={styles.helperText}>
            Allowed 2-30 chars (alphabets, numbers)
          </Typography>

```
### 3. Change "CR/DR" to "Credit/Debit" (Frontend)
Find getTxnTypeColor at the top of the file (around line 89).
**Find this:**
```javascript
const getTxnTypeColor = (txnType) =>
  txnType === "CR" ? "error" : "success";

```
**Replace it with:**
```javascript
const getTxnTypeColor = (txnType) =>
  txnType === "Credit" ? "error" : "success";

```
Find the transactionType mapping in fetchBatchDetailsPaginated (around line 240).
**Find this:**
```javascript
              transactionType: amountVal < 0 ? "CR" : "DR",

```
**Replace it with:**
```javascript
              transactionType: amountVal < 0 ? "Credit" : "Debit",

```
### 4. Excel Download Sequence & Credit/Debit (Backend)
Open your Java backend file: **JournalRequestServiceImpl.java**.
Find the generateBatchExcel method. We need to swap the headers array to put Product before Remarks, and update the row cell populations.
**Replace the entire generateBatchExcel method with this:**
```java
    @Override
    public byte[] generateBatchExcel(String batchId) {
        // Fetch all rows belonging to this batch
        List<JournalRequest> requests = journalRequestRepository.findByBatchId(batchId);
        
        if (requests == null || requests.isEmpty()) {
            throw new CustomExceptions.InvalidDataException("No records found for batch: " + batchId);
        }

        try (org.apache.poi.xssf.usermodel.XSSFWorkbook workbook = new org.apache.poi.xssf.usermodel.XSSFWorkbook();
             java.io.ByteArrayOutputStream out = new java.io.ByteArrayOutputStream()) {

            org.apache.poi.ss.usermodel.Sheet sheet = workbook.createSheet("Batch Details");

            // 1. Create Header Row (Product before Remarks)
            org.apache.poi.ss.usermodel.Row headerRow = sheet.createRow(0);
            String[] headers = {"Branch", "Currency", "CGL", "Amount", "Type", "Product Code", "Remarks", "Date"};
            
            for (int i = 0; i < headers.length; i++) {
                org.apache.poi.ss.usermodel.Cell cell = headerRow.createCell(i);
                cell.setCellValue(headers[i]);
            }

            // 2. Populate Data Rows
            int rowIdx = 1;
            for (JournalRequest req : requests) {
                org.apache.poi.ss.usermodel.Row row = sheet.createRow(rowIdx++);
                
                row.createCell(0).setCellValue(req.getBranchCode() != null ? req.getBranchCode() : "");
                row.createCell(1).setCellValue(req.getCurrency() != null ? req.getCurrency() : "");
                row.createCell(2).setCellValue(req.getCgl() != null ? req.getCgl() : "");

                BigDecimal amount = req.getAmount();
                if (amount != null) {
                    row.createCell(3).setCellValue(amount.abs().doubleValue());
                    // Ensured explicitly to print Credit/Debit
                    row.createCell(4).setCellValue(amount.compareTo(BigDecimal.ZERO) < 0 ? "Credit" : "Debit");
                } else {
                    row.createCell(3).setCellValue(0);
                    row.createCell(4).setCellValue("");
                }

                // Swapped indices: Product is now index 5, Remarks is index 6
                row.createCell(5).setCellValue(req.getProductCode() != null ? req.getProductCode() : "");
                row.createCell(6).setCellValue(req.getNarration() != null ? req.getNarration() : "");
                
                row.createCell(7).setCellValue(req.getCsvDate() != null ? req.getCsvDate().toString() : "");
            }

            workbook.write(out);
            return out.toByteArray();

        } catch (java.io.IOException e) {
            log.error("EXCEL_GEN_ERROR | Batch: {} | Error: {}", batchId, e.getMessage());
            throw new CustomExceptions.DatabaseOperationException("Failed to generate Excel file", e);
        }
    }

```
