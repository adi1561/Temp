That is a great call. Since we combined the validation and submission into one single action, that top stepper is completely obsolete and just taking up valuable screen space.
Here is the exact block to delete to remove it.
Open **JournalBulkUpload.js** and scroll down to your return statement (around line 380).
**Find and DELETE this entire block:**
```javascript
        <Box sx={{ width: "100%", maxWidth: "90%", margin: "0 auto", mb: 4 }}>
          <Stepper activeStep={activeStep}>
            {mainSteps.map((label) => (
              <Step key={label}>
                <StepLabel>{label}</StepLabel>
              </Step>
            ))}
          </Stepper>
        </Box>

```
*(You can also scroll up to the top of the file around line 85 and delete const mainSteps = ["Upload & Validate", "Preview & Submit"]; since it is no longer being used!)*
