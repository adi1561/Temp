The problem is a hidden JavaScript logic bug right inside your conditional check for rendering the button at the very bottom of your file.
Look carefully at how you wrote your visibility check:
```javascript
{isViewingSelfCreated && !isBatchDeletable && ( ... )}

```
You used !isBatchDeletable (Not Deletable). This means you are telling the UI: *"If the batch is **NOT** deletable, show the Delete button!"* That is exactly why the button disappears on your brand-new requests but suddenly displays on your approved or processed batches.
Additionally, when you extract data inside handleOpenViewModal, you are setting the state correctly using setViewingBatchOrder(row.currentOrder);, but your condition is reading from the single state variable viewingBatchOrder.
Here are the precise fixes to apply to your **VoucherPostingStatus.js** file.
### 1. Fix the Visibility Logic Check (Sticky Bottom Bar)
Scroll down to the very bottom of your file inside the <DialogActions> layout block (around line 520) and remove the extra exclamation mark ! so it evaluates the truthy condition correctly.
**Replace this block:**
```javascript
            {/* FIX: Completely hide the button if the batch is not PENDING. */}
            {isViewingSelfCreated && !isBatchDeletable && (
              <Button
                variant="contained"
                color="error"
                startIcon={<DeleteIcon />}
                onClick={() => {
                  setIsViewModalOpen(false); // Dismount detail modal view
                  handleOpenModal({ batchId: viewingBatchId }); // Initialize confirmation dialog box stages
                }}
                sx={{ minWidth: 160, fontWeight: 'bold', py: 1 }}
              >
                Delete Request
              </Button>
            )}

```
**With this corrected block:**
```javascript
            {/* FIX: Changed !isBatchDeletable to isBatchDeletable so it only displays when eligible */}
            {isViewingSelfCreated && isBatchDeletable && (
              <Button
                variant="contained"
                color="error"
                startIcon={<DeleteIcon />}
                onClick={() => {
                  setIsViewModalOpen(false); // Dismount detail modal view
                  handleOpenModal({ batchId: viewingBatchId }); // Initialize confirmation dialog box stages
                }}
                sx={{ minWidth: 160, fontWeight: 'bold', py: 1 }}
              >
                Delete Request
              </Button>
            )}

```
### 2. Defensive Failsafe for isBatchDeletable State Evaluation
To make absolutely sure no string-to-number mismatches break your state check (e.g., if the backend passes the integer 2 vs a string "2"), let's cast it securely.
Find your isBatchDeletable variable definition (around line 255) and replace it with this cast-protected version:
```javascript
  // Cast viewingBatchOrder explicitly to a Number to handle string/numeric type fluidly
  const isBatchDeletable =
    (viewingBatchState?.trim().toUpperCase() === "PENDING" ||
     viewingBatchState?.trim().toUpperCase() === "P") &&
    Number(viewingBatchOrder) === 2;

```
