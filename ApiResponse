Understood. As a Technical Lead, I agree that targeted diffs are much safer to apply.
Here are the exact snippets to change in **JournalBulkValidationService.java** without altering the rest of your methods.
### 1. Add this small helper method (paste it right above parseCsvStream):
```java
    private String padBranch(String branch) {
        if (branch == null || branch.trim().isEmpty()) return "";
        String b = branch.trim();
        if (b.length() < 5) {
            return String.format("%5s", b).replace(' ', '0');
        }
        return b;
    }

```
### 2. Changes inside parseCsvStream
You have two places in this method where d.branch is assigned (one for the "01" system format, one for the standard format).
**Find this line (System format):**
```java
                    if (c.length > 4) d.branch = c[4].trim();

```
**Replace with:**
```java
                    if (c.length > 4) d.branch = padBranch(c[4]);

```
**Find this line (Standard format):**
```java
                    if (c.length > 0) d.branch = c[0].trim();

```
**Replace with:**
```java
                    if (c.length > 0) d.branch = padBranch(c[0]);

```
### 3. Changes inside parseExcelStream
Similarly, you have two places in the Excel parser where d.branch is assigned.
**Find this line (System format):**
```java
                    d.branch = parseCode(r.getCell(4), fmt);

```
**Replace with:**
```java
                    d.branch = padBranch(parseCode(r.getCell(4), fmt));

```
**Find this line (Standard format):**
```java
                    d.branch = col0;

```
**Replace with:**
```java
                    d.branch = padBranch(col0);

```
That's it! By wrapping those 4 specific lines, your branch codes will be dynamically padded to exactly 5 digits the millisecond the file is parsed, feeding perfectly into your DB validation and CSV generation.
