I know exactly why this is happening! If you look in your console, you will see an error saying **ReferenceError: setWorkflowStepsMap is not defined**.
When you copied the fetchMyBatches logic from your Checker screen over to this Status screen, it included the logic to fetch the dynamic workflow steps and save them using setWorkflowStepsMap(stepsMap);. However, **you forgot to declare that state variable at the top of your component!** Because the state doesn't exist, React crashes when trying to save the data, falls into the catch (err) block, and triggers the "Failed to load" snackbar.
Here is the quick 1-line fix:
### The Fix
**FIND your state declarations at the top of the component (around line 125), and ADD the missing state:**
```javascript
  // View/Paginate States
  const [isViewModalOpen, setIsViewModalOpen] = useState(false);
  const [viewingBatchId, setViewingBatchId] = useState(null);
  const [viewingBatchState, setViewingBatchState] = useState("");
  const [viewingBatchCreatorId, setViewingBatchCreatorId] = useState(null);
  const [viewingBatchRequestDate, setViewingBatchRequestDate] = useState(null);
  const [viewingBatchRequestCount, setViewingBatchRequestCount] = useState(0);
  
  // ---> ADD THIS MISSING LINE RIGHT HERE <---
  const [workflowStepsMap, setWorkflowStepsMap] = useState({}); 

  const [vidDetails, setVidDetails] = useState(null);
  const [dynamicSteps, setDynamicSteps] = useState([]);

```
Once you add that line, fetchMyBatches will successfully save the workflow maps, avoid the crash, and your DataGrid will instantly populate with your data!
*(Note: I also noticed you have some leftover "dead" functions from the Checker screen like handleCancelSelectedJournals, handleDetailPageChange, and handleSelectAllClick that reference variables that don't exist here. They won't crash your app because they are never triggered, but you can safely delete them from lines 343 to 380 to keep your code clean!)*
