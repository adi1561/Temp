These are the final "wiring" issues that happen when breaking apart classes. The methods exist, they just got moved or called incorrectly.
Here are the exact replacements to fix all three errors in **VoucherAsyncWorker.java**.
### 1. Fix Cannot resolve method 'getValidRowsFromCache'
We moved the Redis cache logic to ValidationStateManager earlier, so VoucherAsyncWorker needs to use that instead of VoucherBulkValidationService.
**Step A:** At the top of your file in the dependency injection section (around line 53), replace VoucherBulkValidationService with ValidationStateManager:
```java
    // DELETE THIS LINE:
    // private final VoucherBulkValidationService voucherBulkValidationService;

    // ADD THIS LINE INSTEAD:
    private final com.fincore.VoucherService.Service.validation.ValidationStateManager validationStateManager;

```
**Step B:** In the executeAsyncBatchCreation method (around line 100), change the call to use the state manager:
```java
    // DELETE THIS LINE:
    // List<ExcelRowData> cachedRows = voucherBulkValidationService.getValidRowsFromCache(requestId);

    // ADD THIS LINE INSTEAD:
    List<ExcelRowData> cachedRows = validationStateManager.getValidRowsFromCache(requestId);

```
### 2. Fix Cannot resolve method 'logWorkflowAction'
Around line 386 in your executeApprovalInTransaction method, the code tries to call logWorkflowAction directly, but that method belongs to the workflowEngine.
**Find this exact line:**
```java
logWorkflowAction(batchId, vid, "Execution Completed", "SYSTEM", "Automated background execution completed successfully.", null);

```
**Replace it with:**
```java
workflowEngine.logWorkflowAction(batchId, vid, "Execution Completed", "SYSTEM", "Automated background execution completed successfully.", null);

```
### 3. Fix Cannot resolve method 'getVidByBatchId'
Around line 410 in the handleAsyncApprovalError method, it tries to fetch the VID from the workflow engine. Let's just grab it directly from the database using your jdbcTemplate since it's already injected in this file.
**Find this exact line:**
```java
String vid = workflowEngine.getVidByBatchId(batchId);

```
**Replace it with this safe database query:**
```java
        String vid = null;
        try {
            vid = jdbcTemplate.queryForObject("SELECT VID_FK FROM VOUCHER_POSTING WHERE BATCH_ID = ?", String.class, batchId);
        } catch (org.springframework.dao.EmptyResultDataAccessException e) {
            log.warn("VID not found for Batch: {}", batchId);
        }

```
Once you apply these exact replacements, all compilation errors in VoucherAsyncWorker.java will disappear. We are officially ready to do the Controllers! Let me know when you've pasted these fixes.
