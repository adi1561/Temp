You are totally right! I see why you couldn't find the logWorkflowAction SQL snippet.
You actually wrote that method using **Spring Data JPA** (voucherWorkflowRepository.save(wf);) instead of a raw JdbcTemplate SQL query. Because you are using JPA there, you already applied the fix perfectly! You added wf.setScheduledTime(scheduledTime); right before the save command, which does exactly what the raw SQL would have done. You can check that one off the list.
However, you are absolutely correct about the old tables (JOURNAL_BATCH_MASTER and JOURNAL_REQUEST). I scanned through your file, and they are still hiding in several places, which will cause your app to crash with ORA-00942 errors.
Here are the exact replacements you need to make to completely wipe out the old tables and redundant columns from this file.
### 1. Fix executeAsyncBatchCreation (Remove Master Insert)
You are currently deleting and inserting into the old tables, *and* saving the new VoucherPosting JPA entity. We need to drop the old table queries entirely.
**FIND this block (around line 133):**
```java
        // 2. IDEMPOTENCY
        jdbcTemplate.update("DELETE FROM JOURNAL_REQUEST WHERE BATCH_ID = ?", batchId);
        jdbcTemplate.update("DELETE FROM JOURNAL_BATCH_MASTER WHERE BATCH_ID = ?", batchId);

```
**REPLACE with the new tables:**
```java
        // 2. IDEMPOTENCY
        jdbcTemplate.update("DELETE FROM VOUCHER_POSTING_DATA WHERE BATCH_ID = ?", batchId);
        jdbcTemplate.update("DELETE FROM VOUCHER_POSTING WHERE BATCH_ID = ?", batchId);

```
**FIND this block (around line 167) and DELETE IT COMPLETELY:**
```java
        // 6. INTEGRITY & FINAL RECORD
        int actualCommittedRows = successfulRowsCount.get();
        Timestamp ts = Timestamp.valueOf(LocalDateTime.now());
        jdbcTemplate.update("INSERT INTO JOURNAL_BATCH_MASTER (BATCH_ID, CREATOR_ID, REQ_DATE, BATCH_REMARKS, TOTAL_ROWS, TOTAL_DEBIT, TOTAL_CREDIT, BATCH_STATUS) VALUES (?, ?, ?, ?, ?, ?, ?, ?)",
                batchId, creatorId, ts, commonRemarks, actualCommittedRows, totalDebit.get(), totalCredit.get(), "P");

```
*(You don't need this anymore because your voucherPostingRepository.save(posting); right below it handles creating the master record beautifully!)*
### 2. Fix processChunkRaw (Remove Data Redundancy & Old Table)
We need to point the bulk insert to VOUCHER_POSTING_DATA and remove CREATOR_ROLE and COMMON_BATCH_REMARKS from the child rows to stop the data redundancy.
**FIND your processChunkRaw SQL string (around line 228):**
```java
        String sql = "INSERT INTO JOURNAL_REQUEST (" +
                "REQ_ID, REQ_STATUS, REQ_DATE, CREATOR_ID, CREATOR_ROLE, BATCH_ID, " +
                "JOURNAL_ID, COMMON_BATCH_REMARKS, REQ_BRANCH_CODE, REQ_CURRENCY, REQ_CGL, " +
                "REQ_AMOUNT, POSTING_DATE, REQ_NARRATION, REQ_PRODUCT) " +
                "VALUES (JOURNAL_REQUEST_SEQ.NEXTVAL, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)";

```
**REPLACE with the optimized SQL (assuming your new sequence is VOUCHER_POSTING_DATA_SEQ):**
```java
        String sql = "INSERT INTO VOUCHER_POSTING_DATA (" +
                "REQ_ID, REQ_STATUS, REQ_DATE, CREATOR_ID, BATCH_ID, " +
                "JOURNAL_ID, REQ_BRANCH_CODE, REQ_CURRENCY, REQ_CGL, " +
                "REQ_AMOUNT, POSTING_DATE, REQ_NARRATION, REQ_PRODUCT) " +
                "VALUES (VOUCHER_POSTING_DATA_SEQ.NEXTVAL, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)";

```
**FIND the setValues block in that same method and REPLACE the 14 setters with these 12 (since we removed Role and Remarks):**
```java
                    // 2. PERFECTLY MAPPED SETTERS
                    ps.setString(1, "P");                                                           // REQ_STATUS
                    ps.setTimestamp(2, java.sql.Timestamp.valueOf(LocalDateTime.now()));            // REQ_DATE
                    ps.setString(3, creatorId);                                                     // CREATOR_ID
                    ps.setString(4, batchId);                                                       // BATCH_ID
                    ps.setString(5, jId);                                                           // JOURNAL_ID
                    ps.setString(6, r.branch);                                                      // REQ_BRANCH_CODE
                    ps.setString(7, r.currency);                                                    // REQ_CURRENCY
                    ps.setString(8, r.cgl);                                                         // REQ_CGL
                    ps.setBigDecimal(9, signedAmt);                                                 // REQ_AMOUNT
                    ps.setDate(10, java.sql.Date.valueOf(rDate));                                   // POSTING_DATE
                    ps.setString(11, r.remarks);                                                    // REQ_NARRATION
                    ps.setString(12, r.productCode);                                                // REQ_PRODUCT

```
### 3. Fix handleAsyncFailure
We need to revert failures cleanly using the new tables.
**FIND handleAsyncFailure (around line 206) and REPLACE the inside logic:**
```java
    private void handleAsyncFailure(String batchId, String creatorId, String requestId, String message) {
        try {
            // Delete orphaned data rows
            jdbcTemplate.update("DELETE FROM VOUCHER_POSTING_DATA WHERE BATCH_ID = ?", batchId);

            // Revert master record to Error
            int updated = jdbcTemplate.update(
                    "UPDATE VOUCHER_POSTING SET BATCH_STATUS = 'ERROR' WHERE BATCH_ID = ?", batchId);

         } catch (DataAccessException e) {
            log.error("CLEANUP_CRITICAL_FAIL | Batch: {} | Cannot record failure state", batchId);
        }
        progressService.sendProgress(batchId, creatorId, 0, "FAILED", message);
        sendNotification(creatorId, null, "Batch creation Failed for Batch ID : " + batchId, batchId);
    }

```
### 4. Fix getAllRequestsSummary
This is still querying JOURNAL_BATCH_MASTER.
**FIND getAllRequestsSummary (around line 720) and REPLACE the query and map:**
```java
    @Override
    public List<Map<String, Object>> getAllRequestsSummary() {
        String sql = """
            SELECT 
                p.BATCH_ID, p.CREATED_BY AS CREATOR_ID, p.CREATED_AT AS REQ_DATE, 
                p.COMMON_BATCH_REMARKS AS BATCH_REMARKS, 
                p.RECORDS_COUNT AS TOTAL_ROWS, 
                p.BATCH_STATUS, w.WORKFLOW_STAGE, w.WORKFLOW_CODE
            FROM VOUCHER_POSTING p
            LEFT JOIN WORKFLOW_TRANSITIONS w ON p.PENDING_AT = w.WORKFLOW_ID
            ORDER BY p.CREATED_AT DESC
        """;

        return jdbcTemplate.query(sql, (rs, rowNum) -> {
            Map<String, Object> map = new HashMap<>();
            map.put("batchId", rs.getString("BATCH_ID"));
            map.put("creatorId", rs.getString("CREATOR_ID"));
            map.put("requestDate", rs.getTimestamp("REQ_DATE"));
            map.put("commonBatchRemarks", rs.getString("BATCH_REMARKS"));
            map.put("requestCount", rs.getLong("TOTAL_ROWS"));
            map.put("approvalWorkflow", rs.getString("WORKFLOW_CODE"));

            String bStatus = rs.getString("BATCH_STATUS");
            if ("R".equals(bStatus)) {
                map.put("currentStatus", "REJECTED");
            } else if ("A".equals(bStatus) || "C".equals(bStatus)) {
                map.put("currentStatus", "POSTED");
            } else {
                map.put("currentStatus", rs.getString("WORKFLOW_STAGE") != null ? rs.getString("WORKFLOW_STAGE") : "PENDING");
            }

            return map;
        });
    }

```
### 5. Fix getRequestCountByBatchId
Still hitting the old master table.
**FIND getRequestCountByBatchId (around line 431) and REPLACE with:**
```java
    @Override
    public long getRequestCountByBatchId(String batchId) {
        try {
            Long result = jdbcTemplate.queryForObject(
                    "SELECT NVL(RECORDS_COUNT, 0) FROM VOUCHER_POSTING WHERE BATCH_ID = ?", Long.class, batchId);
            return result != null ? result : 0L;
        } catch (EmptyResultDataAccessException e) {
            return 0L;
        } catch (DataAccessException e) {
            return 0L;
        }
    }

```
