Since you are working in a **Senior/Technical Lead** capacity, I’ve refined these files to ensure they perfectly align with your existing architecture—specifically leveraging your `GenericSpecification`, `PaginatedResponseDto`, and `ApiResponse` patterns.

Here are the updated and new files to handle the **Common Request** data retrieval.

---

### 1. The Entity Model

**File:** `com.tcs.fincore.CommonMasterService.model.CommonRequest.java`
This maps your Oracle table structure. I've used `@Lob` for the CLOB fields and properly named the columns to match your schema.

```java
package com.tcs.fincore.CommonMasterService.model;

import jakarta.persistence.*;
import lombok.*;
import org.hibernate.annotations.CreationTimestamp;
import java.time.LocalDateTime;

@Getter
@Setter
@Entity
@AllArgsConstructor
@NoArgsConstructor
@Builder
@Table(name = "COMMON_REQUEST")
public class CommonRequest {

    @Id
    @GeneratedValue(strategy = GenerationType.SEQUENCE, generator = "common_req_seq")
    @SequenceGenerator(name = "common_req_seq", sequenceName = "COMMON_REQ_SEQ", allocationSize = 1)
    @Column(name = "REQ_ID")
    private Long id;

    @Column(name = "REQ_TYPE", length = 20, nullable = false)
    private String requestType;

    @Column(name = "CHANGE_TYPE", length = 2, nullable = false)
    private String changeType;

    @Column(name = "REQ_STATUS", length = 2, nullable = false)
    private String status;

    @CreationTimestamp
    @Column(name = "REQ_DATE", nullable = false, updatable = false)
    private LocalDateTime requestDate;

    @Column(name = "CREATOR_ID", length = 12, nullable = false)
    private String creatorId;

    @Column(name = "EXECUTION_DATE")
    private LocalDateTime executionDate;

    @Lob
    @Column(name = "EXECUTION_REMARKS")
    private String executionRemarks;

    @Column(name = "EXECUTOR_ID", length = 12)
    private String executorId;

    @Column(name = "EXECUTOR_REMARKS", length = 500)
    private String executorRemarks;

    @Lob
    @Column(name = "PAYLOAD")
    private String payload;

    @Column(name = "TARGET_ID", length = 4000)
    private String targetId;

    @Column(name = "CREATOR_REMARKS", length = 500)
    private String creatorRemarks;
}

```

---

### 2. The Data Transfer Object (DTO)

**File:** `com.tcs.fincore.CommonMasterService.dto.CommonRequestDto.java`
I added `@JsonFormat` for the dates to match the UI requirements of your other modules.

```java
package com.tcs.fincore.CommonMasterService.dto;

import com.fasterxml.jackson.annotation.JsonFormat;
import lombok.AllArgsConstructor;
import lombok.Builder;
import lombok.Data;
import lombok.NoArgsConstructor;
import java.time.LocalDateTime;

@Data
@Builder
@NoArgsConstructor
@AllArgsConstructor
public class CommonRequestDto {
    private Long id;
    private String requestType;
    private String changeType;
    private String status;
    
    @JsonFormat(pattern = "yyyy-MM-dd HH:mm:ss")
    private LocalDateTime requestDate;
    
    private String creatorId;
    private String creatorRemarks;
    
    @JsonFormat(pattern = "yyyy-MM-dd HH:mm:ss")
    private LocalDateTime executionDate;
    
    private String executionRemarks;
    private String executorId;
    private String executorRemarks;
    private String payload;
    private String targetId;
}

```

---

### 3. The Repository

**File:** `com.tcs.fincore.CommonMasterService.repository.CommonRequestRepository.java`
Crucially, this extends `JpaSpecificationExecutor` so your `GenericSpecification` works out of the box.

```java
package com.tcs.fincore.CommonMasterService.repository;

import com.tcs.fincore.CommonMasterService.model.CommonRequest;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.data.jpa.repository.JpaSpecificationExecutor;
import org.springframework.stereotype.Repository;

@Repository
public interface CommonRequestRepository extends JpaRepository<CommonRequest, Long>, JpaSpecificationExecutor<CommonRequest> {
}

```

---

### 4. Service Layer Updates

You need to add the method signatures to your interface and then implement them.

**File:** `com.tcs.fincore.CommonMasterService.service.CommonMasterService.java`

```java
// Add these to your existing interface
PaginatedResponseDto<CommonRequestDto> searchCommonRequests(SearchRequest request);
CommonRequestDto getRequestById(Long id);

```

**File:** `com.tcs.fincore.CommonMasterService.service.CommonMasterServiceImpl.java`
Inject the new repository and add these implementations:

```java
// 1. Inject the repository (Add to constructor or @Autowired)
private final CommonRequestRepository commonRequestRepository;

@Override
public PaginatedResponseDto<CommonRequestDto> searchCommonRequests(SearchRequest request) {
    // Default Sort if not provided
    Sort.Direction direction = Sort.Direction.fromString(
            request.getSortOrder() != null ? request.getSortOrder() : "DESC");
    String sortField = request.getSortField() != null ? request.getSortField() : "requestDate";

    PageRequest pageRequest = PageRequest.of(request.getPage(), request.getSize(), Sort.by(direction, sortField));

    // Reuse your GenericSpecification logic
    Page<CommonRequest> page = commonRequestRepository.findAll(
            GenericSpecification.build(request.getFilters()), 
            pageRequest
    );

    List<CommonRequestDto> dtoList = page.getContent().stream()
            .map(this::mapToRequestDto)
            .toList();

    return new PaginatedResponseDto<>(
            page.getNumber(), 
            page.getTotalPages(), 
            page.getSize(), 
            page.getTotalElements(), 
            direction.name(), 
            dtoList
    );
}

@Override
public CommonRequestDto getRequestById(Long id) {
    return commonRequestRepository.findById(id)
            .map(this::mapToRequestDto)
            .orElseThrow(() -> new NoDataFoundException("Request not found with ID: " + id));
}

// Mapper Helper
private CommonRequestDto mapToRequestDto(CommonRequest entity) {
    return CommonRequestDto.builder()
            .id(entity.getId())
            .requestType(entity.getRequestType())
            .changeType(entity.getChangeType())
            .status(entity.getStatus())
            .requestDate(entity.getRequestDate())
            .creatorId(entity.getCreatorId())
            .creatorRemarks(entity.getCreatorRemarks())
            .executionDate(entity.getExecutionDate())
            .executionRemarks(entity.getExecutionRemarks())
            .executorId(entity.getExecutorId())
            .executorRemarks(entity.getExecutorRemarks())
            .payload(entity.getPayload())
            .targetId(entity.getTargetId())
            .build();
}

```

---

### 5. The New Controller

**File:** `com.tcs.fincore.CommonMasterService.Controller.CommonRequestController.java`
This follows your `RestController` pattern.

```java
package com.tcs.fincore.CommonMasterService.Controller;

import com.tcs.fincore.CommonMasterService.dto.ApiResponse;
import com.tcs.fincore.CommonMasterService.dto.CommonRequestDto;
import com.tcs.fincore.CommonMasterService.dto.PaginatedResponseDto;
import com.tcs.fincore.CommonMasterService.dto.SearchRequest;
import com.tcs.fincore.CommonMasterService.service.CommonMasterService;
import lombok.RequiredArgsConstructor;
import lombok.extern.slf4j.Slf4j;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.*;

@Slf4j
@RestController
@RequestMapping("/api/master-requests")
@RequiredArgsConstructor
public class CommonRequestController {

    private final CommonMasterService service;

    /**
     * Search Change Requests with Pagination and Dynamic Filtering
     */
    @PostMapping("/search")
    public ResponseEntity<ApiResponse<PaginatedResponseDto<CommonRequestDto>>> searchRequests(
            @RequestBody SearchRequest request) {
        log.info("Fetching master change requests");
        return ResponseEntity.ok(ApiResponse.success(service.searchCommonRequests(request)));
    }

    /**
     * Fetch Single Request Details by ID
     */
    @GetMapping("/{id}")
    public ResponseEntity<ApiResponse<CommonRequestDto>> getDetails(@PathVariable Long id) {
        log.info("Fetching details for request ID: {}", id);
        return ResponseEntity.ok(ApiResponse.success(service.getRequestById(id)));
    }
}

```

### Key Highlights for your Lead review:

* **Dynamic Filtering**: By using `GenericSpecification.build(request.getFilters())`, the UI can filter by `status` (e.g., 'P'), `creatorId`, or `requestType` without any backend changes.
* **Response Consistency**: The data is wrapped in `ApiResponse`, ensuring the `ApiResponseWrapper` doesn't double-wrap it (per your existing logic).
* **Large Data Handling**: Since `PAYLOAD` and `EXECUTION_REMARKS` are CLOBs, they are mapped via `@Lob`, which is standard for Oracle/JPA integrations.

Would you like me to help you create a sample **JSON Request** to test the search API in Postman?
