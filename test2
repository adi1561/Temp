To create a testing payload for your LHO Transfer request, we must align the JSON structure with your existing CreateRequestDto [cite: 263-265]. [cite_start]Since you specifically mentioned wanting to save to the common request table and using the polymorphic payload structure, you need to provide the requestType, changeType, and a payload object that implements your marker interface.
LHO Transfer Testing Payload
This payload represents a new ADD request for the LHO Transfer batch.
{
  "requestType": "LHO_TRANSFER",
  "changeType": "ADD",
  "targetId": "BATCH-2026-001",
  "payload": {
    "batchId": "BATCH-2026-001",
    "processDate": "2026-02-18",
    "totalAmount": 113000.50,
    "totalRecordCount": 3,
    "circleWiseDetails": [
      {
        "transferDate": "2026-02-18",
        "circleBranchCode": 101,
        "totalProfitLoss": 50000.00
      },
      {
        "transferDate": "2026-02-18",
        "circleBranchCode": 102,
        "totalProfitLoss": 75000.50
      },
      {
        "transferDate": "2026-02-18",
        "circleBranchCode": 103,
        "totalProfitLoss": -12000.00
      }
    ]
  }
}

Implementation Checklist for the Payload
To make the above payload work with your current system, ensure these two classes exist in your project:
1. The Marker Interface
The payload must implement your Payload interface to be accepted by the CreateRequestDto.
package com.tcs.fincore.CommonRequestService.dto.payload;

public class LhoTransferPayload implements Payload {
    private String batchId;
    private String processDate;
    private double totalAmount;
    private int totalRecordCount;
    private List<Map<String, Object>> circleWiseDetails;
    // Getters and Setters
}

2. Update SubType Mapping
Your CreateRequestDto uses @JsonSubTypes to determine which class to instantiate. You must add the entry for LHO_TRANSFER as shown below:
@JsonSubTypes({
    // ... existing types
    @JsonSubTypes.Type(value = LhoTransferPayload.class, name = "LHO_TRANSFER")
})
[span_7](start_span)private Payload payload; [cite: 280-282]

Testing via Postman/cURL
You can test this by hitting your existing endpoint:
 * [cite_start]URL: POST /api/create-request
 * Headers: * Authorization: Bearer <your_token>
   * Content-Type: application/json
Would you like me to provide the Java code for the LhoTransferPayload DTO including all the specific fields from your previous files?
