 @Query("SELECT new com.fincore.YearEndService.DTO.ProfitLossSummaryDto(" +
            "p.transferDate, p.circleBranchCode, SUM(p.profitLoss)) " +
            "FROM ProfitLossTransfer p " +
            "GROUP BY p.circleBranchCode, p.transferDate " +
            "ORDER BY p.transferDate DESC")
i think this is wrong way to write a query is it ???
new com.fincore.YearEndService.DTO.ProfitLossSummaryDto becose of this 
