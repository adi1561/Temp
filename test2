SELECT
	RECON_RUN_DATE, 
	BRANCH_CODE,
	CURRENCY,
	CGL,
	CBS_BALANCE,
	GL_BALANCE,
	DIFFERENCE_AMOUNT,
	DIFF_BW_YESTERDAY,
	DIFFERENCE.TYPE,
	DIFFERENCE.HEAD
FROM
	DIFFERENCE

WHERE
	DIFFERENCE.RECON_RUN_DATE = TO_DATE( $P{date} , 'dd/MM/yyyy') AND DIFFERENCE_AMOUNT != 0

in this there is change like i want to add the new column like ERROR DATE in which follwing is condtions for the row each report
in head column for this if the value is "New Entry" then the ERROR DATE Should be the date $P{date}  for that row 
in other case add random date b/w the 1 week as u know this condtion 
example
if i give u date as 20/11/2025 then the date is  for error column for that row is 
is from 13/11/2025 to 20/11/2025

add this 2 condtions and give mi the date 
