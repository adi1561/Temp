I have analyzed your code across the different layers to verify the column sequence.

You want to enforce the exact sequence:

1. Branch
2. Currency
3. CGL
4. Amount
5. Type
6. **Remarks**
7. **Product Code**

Currently, your application has a **mismatch**. Your Excel Parser and your Error Report are aligned with your desired sequence, but your **Excel Template Download** and your **Batch Download** are in the wrong order.

Here is the exact breakdown and the code you need to fix it.

---

### 1. The Excel Input Parser (Correct)

In `ExcelStreamingParser.java`, the system correctly expects `Remarks` at index 5 and `Product Code` at index 6.
**Status:** ✅ Good. No changes needed.

### 2. The Error Report Download (Correct)

In `ValidationReportGenerator.generateErrorExcelFast()`, the headers and the row data are correctly mapped to print `Remarks` before `Product Code`.
**Status:** ✅ Good. No changes needed.

---

### 3. The Downloaded Template (Incorrect)

In `ValidationReportGenerator.generateExcelTemplateWithGuidelines()`, the headers array and the sample rows generate `Product Code` first, followed by `Remarks`. Because the parser expects the opposite, if a user downloads this template and fills it out, **it will fail validation or insert data into the wrong columns.**

**How to Fix It (`ValidationReportGenerator.java`):**
Find the `generateExcelTemplateWithGuidelines()` method and update the headers and sample row assignments to swap indexes 5 and 6.

```java
// 1. Fix the Headers Array 
String[] headers = {"Branch*", "Currency*", "CGL*", "Amount*", "Type*", "Remarks*", "Product Code"};

// ... further down ...

// 2. Fix Sample Row 1
createStyledCell(sampleRow1, 4, "Dr", sampleStyle);
createStyledCell(sampleRow1, 5, "Sample Remarks", sampleStyle); // Moved Remarks to Index 5
createStyledCell(sampleRow1, 6, "PRD099", sampleStyle);         // Moved Product Code to Index 6

// 3. Fix Sample Row 2
createStyledCell(sampleRow2, 4, "Cr", sampleStyle);
createStyledCell(sampleRow2, 5, "Sample Remarks", sampleStyle); // Moved Remarks to Index 5
createStyledCell(sampleRow2, 6, "PRD099", sampleStyle);         // Moved Product Code to Index 6

```

---

### 4. The Batch Detail Download (Incorrect)

In `VoucherQueryService.generateBatchExcel()`, when a user downloads an existing batch, the code generates `Product Code` before `Remarks`.

**How to Fix It (`VoucherQueryService.java`):**
Find the `generateBatchExcel()` method and swap the column generation for indexes 5 and 6.

```java
// 1. Fix the Headers Array
String[] headers = {"Branch", "Currency", "CGL", "Amount", "Type", "Remarks", "Product Code"};

// ... inside the loop ...

// 2. Fix the Row population
row.createCell(0).setCellValue(req.getBranchCode() != null ? req.getBranchCode() : "");
row.createCell(1).setCellValue(req.getCurrency() != null ? req.getCurrency() : "");
row.createCell(2).setCellValue(req.getCgl() != null ? req.getCgl() : "");
if (req.getAmount() != null) {
    row.createCell(3).setCellValue(req.getAmount().abs().stripTrailingZeros().toPlainString());
    row.createCell(4).setCellValue(req.getAmount().compareTo(BigDecimal.ZERO) < 0 ? "Credit" : "Debit");
}
row.createCell(5).setCellValue(req.getNarration() != null ? req.getNarration() : ""); // Moved Remarks (Narration) to Index 5
row.createCell(6).setCellValue(req.getProductCode() != null ? req.getProductCode() : ""); // Moved Product Code to Index 6

```
